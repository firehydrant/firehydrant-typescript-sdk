/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type RefreshConnectionRequest = {
  slug: string;
  connectionId: string;
};

/** @internal */
export const RefreshConnectionRequest$inboundSchema: z.ZodType<
  RefreshConnectionRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  slug: z.string(),
  connection_id: z.string(),
}).transform((v) => {
  return remap$(v, {
    "connection_id": "connectionId",
  });
});
/** @internal */
export type RefreshConnectionRequest$Outbound = {
  slug: string;
  connection_id: string;
};

/** @internal */
export const RefreshConnectionRequest$outboundSchema: z.ZodType<
  RefreshConnectionRequest$Outbound,
  z.ZodTypeDef,
  RefreshConnectionRequest
> = z.object({
  slug: z.string(),
  connectionId: z.string(),
}).transform((v) => {
  return remap$(v, {
    connectionId: "connection_id",
  });
});

export function refreshConnectionRequestToJSON(
  refreshConnectionRequest: RefreshConnectionRequest,
): string {
  return JSON.stringify(
    RefreshConnectionRequest$outboundSchema.parse(refreshConnectionRequest),
  );
}
export function refreshConnectionRequestFromJSON(
  jsonString: string,
): SafeParseResult<RefreshConnectionRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RefreshConnectionRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RefreshConnectionRequest' from JSON`,
  );
}
