/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scimCreateSCIMUser } from "../../funcs/scimCreateSCIMUser.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateScimUser$inboundSchema,
};

export const tool$scimCreateSCIMUser: ToolDefinition<typeof args> = {
  name: "scim-create-scim-user",
  description: `Create a User from SCIM data

SCIM endpoint to create and provision a new User. This endpoint will provision the User, which allows them to accept their account throught their IDP or via the Forgot Password flow.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scimCreateSCIMUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
