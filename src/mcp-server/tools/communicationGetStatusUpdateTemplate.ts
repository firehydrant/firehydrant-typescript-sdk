/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { communicationGetStatusUpdateTemplate } from "../../funcs/communicationGetStatusUpdateTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStatusUpdateTemplateRequest$inboundSchema,
};

export const tool$communicationGetStatusUpdateTemplate: ToolDefinition<
  typeof args
> = {
  name: "communication-get-status-update-template",
  description: `Get a status update template

Get a single status update template by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await communicationGetStatusUpdateTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
