/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scimPatchSCIMGroup } from "../../funcs/scimPatchSCIMGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchScimGroupRequest$inboundSchema,
};

export const tool$scimPatchSCIMGroup: ToolDefinition<typeof args> = {
  name: "scim-patch-scim-group",
  description: `Partially update a SCIM group

SCIM endpoint to partially update a Team (Colloquial for Group in the SCIM protocol). Supports adding, removing, or replacing members using SCIM PATCH operations.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scimPatchSCIMGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
