/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { alertsUpdateSignalsAlert } from "../../funcs/alertsUpdateSignalsAlert.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSignalsAlertRequest$inboundSchema,
};

export const tool$alertsUpdateSignalsAlert: ToolDefinition<typeof args> = {
  name: "alerts-update-signals-alert",
  description: `Update a Signal alert

Update a Signal alert`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await alertsUpdateSignalsAlert(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
