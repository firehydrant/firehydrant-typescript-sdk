/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesDeleteNuncLink } from "../../funcs/statusPagesDeleteNuncLink.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteNuncLinkRequest$inboundSchema,
};

export const tool$statusPagesDeleteNuncLink: ToolDefinition<typeof args> = {
  name: "status-pages-delete-nunc-link",
  description: `Delete a status page link

Delete a link displayed on a FireHydrant status page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesDeleteNuncLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
