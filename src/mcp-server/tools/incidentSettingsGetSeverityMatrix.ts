/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsGetSeverityMatrix } from "../../funcs/incidentSettingsGetSeverityMatrix.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$incidentSettingsGetSeverityMatrix: ToolDefinition = {
  name: "incident-settings-get-severity-matrix",
  description: `Get severity matrix

Retrieve the Severity matrix for your organization and its components and configured severities.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await incidentSettingsGetSeverityMatrix(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
