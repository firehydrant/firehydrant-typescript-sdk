/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { alertsListIncidentAlerts } from "../../funcs/alertsListIncidentAlerts.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentAlertsRequest$inboundSchema,
};

export const tool$alertsListIncidentAlerts: ToolDefinition<typeof args> = {
  name: "alerts-list-incident-alerts",
  description: `List alerts for an incident

List alerts that have been attached to an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await alertsListIncidentAlerts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
