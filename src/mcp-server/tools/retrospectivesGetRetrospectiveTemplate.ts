/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesGetRetrospectiveTemplate } from "../../funcs/retrospectivesGetRetrospectiveTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRetrospectiveTemplateRequest$inboundSchema,
};

export const tool$retrospectivesGetRetrospectiveTemplate: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-get-retrospective-template",
  description: `Get a retrospective template

Retrieve a single retrospective template by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesGetRetrospectiveTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
