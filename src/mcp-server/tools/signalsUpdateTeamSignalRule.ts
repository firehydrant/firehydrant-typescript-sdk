/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsUpdateTeamSignalRule } from "../../funcs/signalsUpdateTeamSignalRule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTeamSignalRuleRequest$inboundSchema,
};

export const tool$signalsUpdateTeamSignalRule: ToolDefinition<typeof args> = {
  name: "signals-update-team-signal-rule",
  description: `Update a Signals rule

Update a Signals rule by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsUpdateTeamSignalRule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
