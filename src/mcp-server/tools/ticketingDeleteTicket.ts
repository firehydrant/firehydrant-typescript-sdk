/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingDeleteTicket } from "../../funcs/ticketingDeleteTicket.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTicketRequest$inboundSchema,
};

export const tool$ticketingDeleteTicket: ToolDefinition<typeof args> = {
  name: "ticketing-delete-ticket",
  description: `Archive a ticket

Archive a ticket`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingDeleteTicket(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
