/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingUpdateInboundFieldMap } from "../../funcs/ticketingUpdateInboundFieldMap.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateInboundFieldMapRequest$inboundSchema,
};

export const tool$ticketingUpdateInboundFieldMap: ToolDefinition<typeof args> =
  {
    name: "ticketing-update-inbound-field-map",
    description: `Update inbound field map for a ticketing project

Update inbound field map for a ticketing project`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await ticketingUpdateInboundFieldMap(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
