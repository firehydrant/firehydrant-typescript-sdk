/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesGetServiceDependency } from "../../funcs/catalogEntriesGetServiceDependency.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetServiceDependencyRequest$inboundSchema,
};

export const tool$catalogEntriesGetServiceDependency: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-get-service-dependency",
  description: `Get a service dependency

Retrieves a single service dependency by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesGetServiceDependency(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
