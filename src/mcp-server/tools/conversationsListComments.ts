/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationsListComments } from "../../funcs/conversationsListComments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCommentsRequest$inboundSchema,
};

export const tool$conversationsListComments: ToolDefinition<typeof args> = {
  name: "conversations-list-comments",
  description: `List comments for a conversation

List all of the comments that have been added to the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await conversationsListComments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
