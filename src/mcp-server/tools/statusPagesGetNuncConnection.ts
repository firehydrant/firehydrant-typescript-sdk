/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesGetNuncConnection } from "../../funcs/statusPagesGetNuncConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetNuncConnectionRequest$inboundSchema,
};

export const tool$statusPagesGetNuncConnection: ToolDefinition<typeof args> = {
  name: "status-pages-get-nunc-connection",
  description: `Get a status page

Retrieve the information displayed as part of your FireHydrant hosted status page.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesGetNuncConnection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
