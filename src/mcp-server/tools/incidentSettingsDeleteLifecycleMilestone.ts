/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsDeleteLifecycleMilestone } from "../../funcs/incidentSettingsDeleteLifecycleMilestone.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteLifecycleMilestoneRequest$inboundSchema,
};

export const tool$incidentSettingsDeleteLifecycleMilestone: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-delete-lifecycle-milestone",
  description: `Delete a milestone

Delete a milestone`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsDeleteLifecycleMilestone(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
