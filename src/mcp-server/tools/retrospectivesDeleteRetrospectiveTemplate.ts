/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesDeleteRetrospectiveTemplate } from "../../funcs/retrospectivesDeleteRetrospectiveTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteRetrospectiveTemplateRequest$inboundSchema,
};

export const tool$retrospectivesDeleteRetrospectiveTemplate: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-delete-retrospective-template",
  description: `Delete a retrospective template

Delete a single retrospective template`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesDeleteRetrospectiveTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
