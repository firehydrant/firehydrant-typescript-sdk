/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsGetIncidentType } from "../../funcs/incidentSettingsGetIncidentType.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIncidentTypeRequest$inboundSchema,
};

export const tool$incidentSettingsGetIncidentType: ToolDefinition<typeof args> =
  {
    name: "incident-settings-get-incident-type",
    description: `Get an incident type

Retrieve a single incident type from its ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await incidentSettingsGetIncidentType(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
