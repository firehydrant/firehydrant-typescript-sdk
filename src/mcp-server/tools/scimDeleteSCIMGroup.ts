/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scimDeleteSCIMGroup } from "../../funcs/scimDeleteSCIMGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteScimGroupRequest$inboundSchema,
};

export const tool$scimDeleteSCIMGroup: ToolDefinition<typeof args> = {
  name: "scim-delete-scim-group",
  description: `Delete a SCIM group

SCIM endpoint to delete a Team (Colloquial for Group in the SCIM protocol).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scimDeleteSCIMGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
