/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesListServiceAvailableUpstreamDependencies } from "../../funcs/catalogEntriesListServiceAvailableUpstreamDependencies.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.ListServiceAvailableUpstreamDependenciesRequest$inboundSchema,
};

export const tool$catalogEntriesListServiceAvailableUpstreamDependencies:
  ToolDefinition<typeof args> = {
    name: "catalog-entries-list-service-available-upstream-dependencies",
    description: `List available upstream service dependencies

Retrieves all services that are available to be upstream dependencies`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await catalogEntriesListServiceAvailableUpstreamDependencies(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
