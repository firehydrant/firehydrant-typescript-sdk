/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationsGetComment } from "../../funcs/conversationsGetComment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCommentRequest$inboundSchema,
};

export const tool$conversationsGetComment: ToolDefinition<typeof args> = {
  name: "conversations-get-comment",
  description: `Get a conversation comment

Retrieves a single comment by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await conversationsGetComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
