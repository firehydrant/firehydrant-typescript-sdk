/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesGetEnvironment } from "../../funcs/catalogEntriesGetEnvironment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetEnvironmentRequest$inboundSchema,
};

export const tool$catalogEntriesGetEnvironment: ToolDefinition<typeof args> = {
  name: "catalog-entries-get-environment",
  description: `Get an environment

Retrieves a single environment by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesGetEnvironment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
