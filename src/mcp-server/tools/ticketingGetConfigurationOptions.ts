/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingGetConfigurationOptions } from "../../funcs/ticketingGetConfigurationOptions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetConfigurationOptionsRequest$inboundSchema,
};

export const tool$ticketingGetConfigurationOptions: ToolDefinition<
  typeof args
> = {
  name: "ticketing-get-configuration-options",
  description: `List configuration options for a ticketing project

List all configuration options for a ticketing project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingGetConfigurationOptions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
