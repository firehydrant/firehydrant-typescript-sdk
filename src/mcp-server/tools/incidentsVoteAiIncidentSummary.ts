/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsVoteAiIncidentSummary } from "../../funcs/incidentsVoteAiIncidentSummary.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.VoteAiIncidentSummaryRequest$inboundSchema,
};

export const tool$incidentsVoteAiIncidentSummary: ToolDefinition<typeof args> =
  {
    name: "incidents-vote-ai-incident-summary",
    description: `Vote on an AI-generated incident summary

Vote on an AI-generated incident summary for the current user`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await incidentsVoteAiIncidentSummary(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
