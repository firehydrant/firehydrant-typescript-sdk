/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesDeleteService } from "../../funcs/catalogEntriesDeleteService.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteServiceRequest$inboundSchema,
};

export const tool$catalogEntriesDeleteService: ToolDefinition<typeof args> = {
  name: "catalog-entries-delete-service",
  description: `Delete a service

Deletes the service from FireHydrant.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesDeleteService(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
