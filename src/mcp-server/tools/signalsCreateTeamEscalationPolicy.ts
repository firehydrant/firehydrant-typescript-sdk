/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsCreateTeamEscalationPolicy } from "../../funcs/signalsCreateTeamEscalationPolicy.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTeamEscalationPolicyRequest$inboundSchema,
};

export const tool$signalsCreateTeamEscalationPolicy: ToolDefinition<
  typeof args
> = {
  name: "signals-create-team-escalation-policy",
  description: `Create an escalation policy for a team

Create a Signals escalation policy for a team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsCreateTeamEscalationPolicy(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
