/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsListScheduledMaintenances } from "../../funcs/incidentsListScheduledMaintenances.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListScheduledMaintenancesRequest$inboundSchema,
};

export const tool$incidentsListScheduledMaintenances: ToolDefinition<
  typeof args
> = {
  name: "incidents-list-scheduled-maintenances",
  description: `List scheduled maintenance events

Lists all scheduled maintenance events`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsListScheduledMaintenances(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
