/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesGetIncidentRetrospectiveField } from "../../funcs/retrospectivesGetIncidentRetrospectiveField.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIncidentRetrospectiveFieldRequest$inboundSchema,
};

export const tool$retrospectivesGetIncidentRetrospectiveField: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-get-incident-retrospective-field",
  description: `Get a retrospective field

Retrieve a field on an incident retrospective`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesGetIncidentRetrospectiveField(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
