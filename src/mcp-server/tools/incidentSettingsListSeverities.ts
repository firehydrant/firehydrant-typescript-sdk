/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsListSeverities } from "../../funcs/incidentSettingsListSeverities.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListSeveritiesRequest$inboundSchema,
};

export const tool$incidentSettingsListSeverities: ToolDefinition<typeof args> =
  {
    name: "incident-settings-list-severities",
    description: `List severities

Lists severities`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await incidentSettingsListSeverities(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
