/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsDeleteIncidentChatMessage } from "../../funcs/incidentsDeleteIncidentChatMessage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteIncidentChatMessageRequest$inboundSchema,
};

export const tool$incidentsDeleteIncidentChatMessage: ToolDefinition<
  typeof args
> = {
  name: "incidents-delete-incident-chat-message",
  description: `Delete a chat message from an incident

Delete an existing generic chat message on an incident.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsDeleteIncidentChatMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
