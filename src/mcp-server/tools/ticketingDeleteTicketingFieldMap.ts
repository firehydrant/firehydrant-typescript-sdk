/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingDeleteTicketingFieldMap } from "../../funcs/ticketingDeleteTicketingFieldMap.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTicketingFieldMapRequest$inboundSchema,
};

export const tool$ticketingDeleteTicketingFieldMap: ToolDefinition<
  typeof args
> = {
  name: "ticketing-delete-ticketing-field-map",
  description: `Archive a field map for a ticketing project

Archive field map for a ticketing project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingDeleteTicketingFieldMap(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
