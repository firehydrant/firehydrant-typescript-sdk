/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { rolesDeleteRole } from "../../funcs/rolesDeleteRole.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteRoleRequest$inboundSchema,
};

export const tool$rolesDeleteRole: ToolDefinition<typeof args> = {
  name: "roles-delete-role",
  description: `Delete a role

Delete a role`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await rolesDeleteRole(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
