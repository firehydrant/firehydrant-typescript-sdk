/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsDeleteNotificationPolicy } from "../../funcs/signalsDeleteNotificationPolicy.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteNotificationPolicyRequest$inboundSchema,
};

export const tool$signalsDeleteNotificationPolicy: ToolDefinition<typeof args> =
  {
    name: "signals-delete-notification-policy",
    description: `Delete a notification policy

Delete a Signals notification policy by ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsDeleteNotificationPolicy(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
