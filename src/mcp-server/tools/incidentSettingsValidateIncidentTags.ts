/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { incidentSettingsValidateIncidentTags } from "../../funcs/incidentSettingsValidateIncidentTags.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: z.array(z.string()),
};

export const tool$incidentSettingsValidateIncidentTags: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-validate-incident-tags",
  description: `Validate incident tags

Validate the format of a list of tags`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsValidateIncidentTags(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
