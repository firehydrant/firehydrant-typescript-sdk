/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesCreateRetrospectiveTemplate } from "../../funcs/retrospectivesCreateRetrospectiveTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateRetrospectiveTemplateRequest$inboundSchema,
};

export const tool$retrospectivesCreateRetrospectiveTemplate: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-create-retrospective-template",
  description: `Create a retrospective template

Create a new retrospective template`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesCreateRetrospectiveTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
