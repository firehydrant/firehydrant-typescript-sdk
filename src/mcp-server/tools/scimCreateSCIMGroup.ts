/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scimCreateSCIMGroup } from "../../funcs/scimCreateSCIMGroup.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateScimGroup$inboundSchema,
};

export const tool$scimCreateSCIMGroup: ToolDefinition<typeof args> = {
  name: "scim-create-scim-group",
  description: `Create a SCIM group and assign members

SCIM endpoint to create a new Team (Colloquial for Group in the SCIM protocol). Any members defined in the payload will be assigned to the team with no defined role.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scimCreateSCIMGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
