/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { audiencesGetAudienceSummary } from "../../funcs/audiencesGetAudienceSummary.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAudienceSummaryRequest$inboundSchema,
};

export const tool$audiencesGetAudienceSummary: ToolDefinition<typeof args> = {
  name: "audiences-get-audience-summary",
  description: `Get latest summary

Get the latest audience-specific summary for an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await audiencesGetAudienceSummary(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
