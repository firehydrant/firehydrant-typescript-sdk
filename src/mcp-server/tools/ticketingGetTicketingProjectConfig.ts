/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingGetTicketingProjectConfig } from "../../funcs/ticketingGetTicketingProjectConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTicketingProjectConfigRequest$inboundSchema,
};

export const tool$ticketingGetTicketingProjectConfig: ToolDefinition<
  typeof args
> = {
  name: "ticketing-get-ticketing-project-config",
  description: `Get configuration for a ticketing project

Retrieve configuration for a ticketing project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingGetTicketingProjectConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
