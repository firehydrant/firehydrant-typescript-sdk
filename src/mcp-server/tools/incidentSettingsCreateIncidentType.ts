/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsCreateIncidentType } from "../../funcs/incidentSettingsCreateIncidentType.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateIncidentType$inboundSchema,
};

export const tool$incidentSettingsCreateIncidentType: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-create-incident-type",
  description: `Create an incident type

Create a new incident type`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsCreateIncidentType(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
