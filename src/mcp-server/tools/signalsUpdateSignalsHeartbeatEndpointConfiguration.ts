/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsUpdateSignalsHeartbeatEndpointConfiguration } from "../../funcs/signalsUpdateSignalsHeartbeatEndpointConfiguration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.UpdateSignalsHeartbeatEndpointConfigurationRequest$inboundSchema,
};

export const tool$signalsUpdateSignalsHeartbeatEndpointConfiguration:
  ToolDefinition<typeof args> = {
    name: "signals-update-signals-heartbeat-endpoint-configuration",
    description: `Update a heartbeat endpoint configuration

Update an existing heartbeat endpoint configuration`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await signalsUpdateSignalsHeartbeatEndpointConfiguration(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
