/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetSignalsWebhookTarget } from "../../funcs/signalsGetSignalsWebhookTarget.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSignalsWebhookTargetRequest$inboundSchema,
};

export const tool$signalsGetSignalsWebhookTarget: ToolDefinition<typeof args> =
  {
    name: "signals-get-signals-webhook-target",
    description: `Get a webhook target

Get a Signals webhook target by ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsGetSignalsWebhookTarget(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
