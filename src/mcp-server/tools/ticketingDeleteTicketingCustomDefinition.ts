/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingDeleteTicketingCustomDefinition } from "../../funcs/ticketingDeleteTicketingCustomDefinition.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTicketingCustomDefinitionRequest$inboundSchema,
};

export const tool$ticketingDeleteTicketingCustomDefinition: ToolDefinition<
  typeof args
> = {
  name: "ticketing-delete-ticketing-custom-definition",
  description: `Delete a ticketing custom field

Deletes a ticketing custom field for the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingDeleteTicketingCustomDefinition(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
