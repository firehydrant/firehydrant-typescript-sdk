/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksDeleteTaskList } from "../../funcs/tasksDeleteTaskList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTaskListRequest$inboundSchema,
};

export const tool$tasksDeleteTaskList: ToolDefinition<typeof args> = {
  name: "tasks-delete-task-list",
  description: `Delete a task list

Delete a task list`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksDeleteTaskList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
