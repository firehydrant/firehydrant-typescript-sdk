/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingDeleteSavedSearch } from "../../funcs/metricsReportingDeleteSavedSearch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSavedSearchRequest$inboundSchema,
};

export const tool$metricsReportingDeleteSavedSearch: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-delete-saved-search",
  description: `Delete a saved search

Delete a specific saved search`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsReportingDeleteSavedSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
