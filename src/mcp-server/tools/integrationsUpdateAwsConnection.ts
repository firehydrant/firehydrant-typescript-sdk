/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsUpdateAwsConnection } from "../../funcs/integrationsUpdateAwsConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAwsConnectionRequest$inboundSchema,
};

export const tool$integrationsUpdateAwsConnection: ToolDefinition<typeof args> =
  {
    name: "integrations-update-aws-connection",
    description: `Update an AWS connection

Update the AWS connection with the provided data.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await integrationsUpdateAwsConnection(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
