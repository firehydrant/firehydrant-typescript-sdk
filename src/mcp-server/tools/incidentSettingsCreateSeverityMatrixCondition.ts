/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsCreateSeverityMatrixCondition } from "../../funcs/incidentSettingsCreateSeverityMatrixCondition.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateSeverityMatrixCondition$inboundSchema,
};

export const tool$incidentSettingsCreateSeverityMatrixCondition: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-create-severity-matrix-condition",
  description: `Create a severity matrix condition

Create a new condition`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await incidentSettingsCreateSeverityMatrixCondition(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
