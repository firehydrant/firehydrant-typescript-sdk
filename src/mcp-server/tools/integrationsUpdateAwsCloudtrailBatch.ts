/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsUpdateAwsCloudtrailBatch } from "../../funcs/integrationsUpdateAwsCloudtrailBatch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAwsCloudtrailBatchRequest$inboundSchema,
};

export const tool$integrationsUpdateAwsCloudtrailBatch: ToolDefinition<
  typeof args
> = {
  name: "integrations-update-aws-cloudtrail-batch",
  description: `Update a CloudTrail batch

Update a CloudTrail batch with new information.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsUpdateAwsCloudtrailBatch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
