/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksCreateRunbookExecution } from "../../funcs/runbooksCreateRunbookExecution.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateRunbookExecution$inboundSchema,
};

export const tool$runbooksCreateRunbookExecution: ToolDefinition<typeof args> =
  {
    name: "runbooks-create-runbook-execution",
    description: `Create a runbook execution

Attaches a runbook to an incident and executes it`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await runbooksCreateRunbookExecution(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
