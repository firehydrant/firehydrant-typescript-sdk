/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsCreateSignalsEmailTarget } from "../../funcs/signalsCreateSignalsEmailTarget.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateSignalsEmailTarget$inboundSchema,
};

export const tool$signalsCreateSignalsEmailTarget: ToolDefinition<typeof args> =
  {
    name: "signals-create-signals-email-target",
    description: `Create an email target for signals

Create a Signals email target for a team.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsCreateSignalsEmailTarget(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
