/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsListSignalsEventSources } from "../../funcs/signalsListSignalsEventSources.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListSignalsEventSourcesRequest$inboundSchema,
};

export const tool$signalsListSignalsEventSources: ToolDefinition<typeof args> =
  {
    name: "signals-list-signals-event-sources",
    description: `List event sources for Signals

List all Signals event sources for the authenticated user.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsListSignalsEventSources(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
