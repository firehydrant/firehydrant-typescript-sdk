/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsListIncidentRoleAssignments } from "../../funcs/incidentsListIncidentRoleAssignments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentRoleAssignmentsRequest$inboundSchema,
};

export const tool$incidentsListIncidentRoleAssignments: ToolDefinition<
  typeof args
> = {
  name: "incidents-list-incident-role-assignments",
  description: `List incident assignees

Retrieve a list of all of the current role assignments for the incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsListIncidentRoleAssignments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
