/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksCreateChecklistTemplate } from "../../funcs/tasksCreateChecklistTemplate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateChecklistTemplate$inboundSchema,
};

export const tool$tasksCreateChecklistTemplate: ToolDefinition<typeof args> = {
  name: "tasks-create-checklist-template",
  description: `Create a checklist template

Creates a checklist template for the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksCreateChecklistTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
