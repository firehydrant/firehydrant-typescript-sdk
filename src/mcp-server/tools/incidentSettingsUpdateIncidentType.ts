/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsUpdateIncidentType } from "../../funcs/incidentSettingsUpdateIncidentType.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateIncidentTypeRequest$inboundSchema,
};

export const tool$incidentSettingsUpdateIncidentType: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-update-incident-type",
  description: `Update an incident type

Update a single incident type from its ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsUpdateIncidentType(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
