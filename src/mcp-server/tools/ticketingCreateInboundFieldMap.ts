/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingCreateInboundFieldMap } from "../../funcs/ticketingCreateInboundFieldMap.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateInboundFieldMapRequest$inboundSchema,
};

export const tool$ticketingCreateInboundFieldMap: ToolDefinition<typeof args> =
  {
    name: "ticketing-create-inbound-field-map",
    description: `Create inbound field map for a ticketing project

Creates inbound field map for a ticketing project`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await ticketingCreateInboundFieldMap(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
