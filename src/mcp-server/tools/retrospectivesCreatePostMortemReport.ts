/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesCreatePostMortemReport } from "../../funcs/retrospectivesCreatePostMortemReport.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreatePostMortemReport$inboundSchema,
};

export const tool$retrospectivesCreatePostMortemReport: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-create-post-mortem-report",
  description: `Create a retrospective report

Create a report`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesCreatePostMortemReport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
