/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksGetChecklistTemplate } from "../../funcs/tasksGetChecklistTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetChecklistTemplateRequest$inboundSchema,
};

export const tool$tasksGetChecklistTemplate: ToolDefinition<typeof args> = {
  name: "tasks-get-checklist-template",
  description: `Get a checklist template

Retrieves a single checklist template by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksGetChecklistTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
