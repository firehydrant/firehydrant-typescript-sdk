/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingListInboundFieldMaps } from "../../funcs/ticketingListInboundFieldMaps.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListInboundFieldMapsRequest$inboundSchema,
};

export const tool$ticketingListInboundFieldMaps: ToolDefinition<typeof args> = {
  name: "ticketing-list-inbound-field-maps",
  description: `List inbound field maps for a ticketing project

List all inbound field maps for a ticketing project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingListInboundFieldMaps(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
