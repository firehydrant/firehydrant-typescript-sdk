/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingCreateTicketingFieldMap } from "../../funcs/ticketingCreateTicketingFieldMap.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTicketingFieldMapRequest$inboundSchema,
};

export const tool$ticketingCreateTicketingFieldMap: ToolDefinition<
  typeof args
> = {
  name: "ticketing-create-ticketing-field-map",
  description: `Create a field mapping for a ticketing project

Creates field map for a ticketing project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingCreateTicketingFieldMap(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
