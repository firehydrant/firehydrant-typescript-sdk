/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsCreateSeverity } from "../../funcs/incidentSettingsCreateSeverity.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateSeverity$inboundSchema,
};

export const tool$incidentSettingsCreateSeverity: ToolDefinition<typeof args> =
  {
    name: "incident-settings-create-severity",
    description: `Create a severity

Create a new severity`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await incidentSettingsCreateSeverity(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
