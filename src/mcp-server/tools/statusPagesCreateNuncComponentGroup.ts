/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesCreateNuncComponentGroup } from "../../funcs/statusPagesCreateNuncComponentGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateNuncComponentGroupRequest$inboundSchema,
};

export const tool$statusPagesCreateNuncComponentGroup: ToolDefinition<
  typeof args
> = {
  name: "status-pages-create-nunc-component-group",
  description: `Create a component group for a status page

Add a component group to be displayed on a FireHydrant status page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesCreateNuncComponentGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
