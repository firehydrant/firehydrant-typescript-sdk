/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsListTranscriptEntries } from "../../funcs/incidentsListTranscriptEntries.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTranscriptEntriesRequest$inboundSchema,
};

export const tool$incidentsListTranscriptEntries: ToolDefinition<typeof args> =
  {
    name: "incidents-list-transcript-entries",
    description: `Lists all of the messages in the incident's transcript

Retrieve the transcript for a specific incident`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await incidentsListTranscriptEntries(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
