/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesUpdateNuncComponentGroup } from "../../funcs/statusPagesUpdateNuncComponentGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateNuncComponentGroupRequest$inboundSchema,
};

export const tool$statusPagesUpdateNuncComponentGroup: ToolDefinition<
  typeof args
> = {
  name: "status-pages-update-nunc-component-group",
  description: `Update a status page component group

Update a component group to be displayed on a FireHydrant status page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesUpdateNuncComponentGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
