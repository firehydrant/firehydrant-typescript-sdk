/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scimPatchSCIMUser } from "../../funcs/scimPatchSCIMUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchScimUserRequest$inboundSchema,
};

export const tool$scimPatchSCIMUser: ToolDefinition<typeof args> = {
  name: "scim-patch-scim-user",
  description: `Update a User from SCIM data

PATCH SCIM endpoint to update a User. This endpoint is used to update a resource's attributes.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scimPatchSCIMUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
