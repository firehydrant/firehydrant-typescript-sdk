/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesDeleteNuncComponentGroup } from "../../funcs/statusPagesDeleteNuncComponentGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteNuncComponentGroupRequest$inboundSchema,
};

export const tool$statusPagesDeleteNuncComponentGroup: ToolDefinition<
  typeof args
> = {
  name: "status-pages-delete-nunc-component-group",
  description: `Delete a status page component group

Delete a component group displayed on a FireHydrant status page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesDeleteNuncComponentGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
