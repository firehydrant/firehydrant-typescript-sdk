/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsUpdateTeam } from "../../funcs/teamsUpdateTeam.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTeamRequest$inboundSchema,
};

export const tool$teamsUpdateTeam: ToolDefinition<typeof args> = {
  name: "teams-update-team",
  description: `Update a team

Update a single team from its ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamsUpdateTeam(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
