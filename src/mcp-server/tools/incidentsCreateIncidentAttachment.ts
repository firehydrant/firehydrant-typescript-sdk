/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsCreateIncidentAttachment } from "../../funcs/incidentsCreateIncidentAttachment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateIncidentAttachmentRequest$inboundSchema,
};

export const tool$incidentsCreateIncidentAttachment: ToolDefinition<
  typeof args
> = {
  name: "incidents-create-incident-attachment",
  description: `Add an attachment to the incident timeline

Allows adding image attachments to an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsCreateIncidentAttachment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
