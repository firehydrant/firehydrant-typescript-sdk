/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsUpdateIncidentChatMessage } from "../../funcs/incidentsUpdateIncidentChatMessage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateIncidentChatMessageRequest$inboundSchema,
};

export const tool$incidentsUpdateIncidentChatMessage: ToolDefinition<
  typeof args
> = {
  name: "incidents-update-incident-chat-message",
  description: `Update a chat message on an incident

Update an existing generic chat message on an incident.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsUpdateIncidentChatMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
