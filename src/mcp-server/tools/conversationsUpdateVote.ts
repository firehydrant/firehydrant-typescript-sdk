/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationsUpdateVote } from "../../funcs/conversationsUpdateVote.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateVoteRequest$inboundSchema,
};

export const tool$conversationsUpdateVote: ToolDefinition<typeof args> = {
  name: "conversations-update-vote",
  description: `Update votes

Upvote or downvote an object`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await conversationsUpdateVote(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
