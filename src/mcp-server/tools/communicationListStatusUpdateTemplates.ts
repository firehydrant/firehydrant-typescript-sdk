/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { communicationListStatusUpdateTemplates } from "../../funcs/communicationListStatusUpdateTemplates.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListStatusUpdateTemplatesRequest$inboundSchema,
};

export const tool$communicationListStatusUpdateTemplates: ToolDefinition<
  typeof args
> = {
  name: "communication-list-status-update-templates",
  description: `List status update templates

List all status update templates for your organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await communicationListStatusUpdateTemplates(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
