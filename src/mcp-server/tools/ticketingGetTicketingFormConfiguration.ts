/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingGetTicketingFormConfiguration } from "../../funcs/ticketingGetTicketingFormConfiguration.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$ticketingGetTicketingFormConfiguration: ToolDefinition = {
  name: "ticketing-get-ticketing-form-configuration",
  description: `Get the ticketing form configuration

Get the ticketing form configuration`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await ticketingGetTicketingFormConfiguration(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
