/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesListFunctionalities } from "../../funcs/catalogEntriesListFunctionalities.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListFunctionalitiesRequest$inboundSchema,
};

export const tool$catalogEntriesListFunctionalities: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-list-functionalities",
  description: `List functionalities

List all of the functionalities that have been added to the organiation`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesListFunctionalities(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
