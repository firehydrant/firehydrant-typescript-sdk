/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesCreateServiceDependency } from "../../funcs/catalogEntriesCreateServiceDependency.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateServiceDependency$inboundSchema,
};

export const tool$catalogEntriesCreateServiceDependency: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-create-service-dependency",
  description: `Create a service dependency

Creates a service dependency relationship between two services`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesCreateServiceDependency(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
