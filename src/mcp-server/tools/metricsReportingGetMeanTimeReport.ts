/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingGetMeanTimeReport } from "../../funcs/metricsReportingGetMeanTimeReport.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetMeanTimeReportRequest$inboundSchema,
};

export const tool$metricsReportingGetMeanTimeReport: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-get-mean-time-report",
  description: `Get mean time metrics for incidents

Returns a report with time bucketed analytics data`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsReportingGetMeanTimeReport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
