/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsUpdateFieldMap } from "../../funcs/integrationsUpdateFieldMap.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateFieldMapRequest$inboundSchema,
};

export const tool$integrationsUpdateFieldMap: ToolDefinition<typeof args> = {
  name: "integrations-update-field-map",
  description: `Update field mapping

Update field mapping`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsUpdateFieldMap(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
