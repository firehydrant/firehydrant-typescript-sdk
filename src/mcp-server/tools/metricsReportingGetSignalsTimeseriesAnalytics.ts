/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingGetSignalsTimeseriesAnalytics } from "../../funcs/metricsReportingGetSignalsTimeseriesAnalytics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSignalsTimeseriesAnalyticsRequest$inboundSchema,
};

export const tool$metricsReportingGetSignalsTimeseriesAnalytics: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-get-signals-timeseries-analytics",
  description: `Generate timeseries alert metrics

Generate a timeseries-based report of metrics for Signals alerts`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await metricsReportingGetSignalsTimeseriesAnalytics(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
