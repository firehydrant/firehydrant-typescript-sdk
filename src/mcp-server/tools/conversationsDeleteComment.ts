/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationsDeleteComment } from "../../funcs/conversationsDeleteComment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteCommentRequest$inboundSchema,
};

export const tool$conversationsDeleteComment: ToolDefinition<typeof args> = {
  name: "conversations-delete-comment",
  description: `Archive a conversation comment

ALPHA - Archive a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await conversationsDeleteComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
