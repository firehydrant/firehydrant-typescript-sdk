/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsCreateLifecycleMilestone } from "../../funcs/incidentSettingsCreateLifecycleMilestone.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLifecycleMilestoneRequest$inboundSchema,
};

export const tool$incidentSettingsCreateLifecycleMilestone: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-create-lifecycle-milestone",
  description: `Create a milestone

Create a new milestone`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsCreateLifecycleMilestone(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
