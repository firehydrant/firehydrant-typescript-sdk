/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingGetSavedSearch } from "../../funcs/metricsReportingGetSavedSearch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSavedSearchRequest$inboundSchema,
};

export const tool$metricsReportingGetSavedSearch: ToolDefinition<typeof args> =
  {
    name: "metrics/reporting-get-saved-search",
    description: `Get a saved search

Retrieve a specific save search`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await metricsReportingGetSavedSearch(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
