/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesListIncidentRetrospectives } from "../../funcs/retrospectivesListIncidentRetrospectives.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentRetrospectivesRequest$inboundSchema,
};

export const tool$retrospectivesListIncidentRetrospectives: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-list-incident-retrospectives",
  description: `All attached retrospectives for an incident

Retrieve retrospectives attached to an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesListIncidentRetrospectives(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
