/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsListConnectionStatusesBySlug } from "../../funcs/integrationsListConnectionStatusesBySlug.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListConnectionStatusesBySlugRequest$inboundSchema,
};

export const tool$integrationsListConnectionStatusesBySlug: ToolDefinition<
  typeof args
> = {
  name: "integrations-list-connection-statuses-by-slug",
  description: `Get an integration connection status

Retrieve a single integration connection status`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsListConnectionStatusesBySlug(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
