/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksListWebhooks } from "../../funcs/webhooksListWebhooks.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListWebhooksRequest$inboundSchema,
};

export const tool$webhooksListWebhooks: ToolDefinition<typeof args> = {
  name: "webhooks-list-webhooks",
  description: `List webhooks

Lists webhooks`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksListWebhooks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
