/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsCreatePriority } from "../../funcs/incidentSettingsCreatePriority.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreatePriority$inboundSchema,
};

export const tool$incidentSettingsCreatePriority: ToolDefinition<typeof args> =
  {
    name: "incident-settings-create-priority",
    description: `Create a priority

Create a new priority`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await incidentSettingsCreatePriority(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
