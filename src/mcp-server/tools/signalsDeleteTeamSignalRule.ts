/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsDeleteTeamSignalRule } from "../../funcs/signalsDeleteTeamSignalRule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTeamSignalRuleRequest$inboundSchema,
};

export const tool$signalsDeleteTeamSignalRule: ToolDefinition<typeof args> = {
  name: "signals-delete-team-signal-rule",
  description: `Delete a Signals rule

Delete a Signals rule by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsDeleteTeamSignalRule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
