/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsGetIncidentRole } from "../../funcs/incidentSettingsGetIncidentRole.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIncidentRoleRequest$inboundSchema,
};

export const tool$incidentSettingsGetIncidentRole: ToolDefinition<typeof args> =
  {
    name: "incident-settings-get-incident-role",
    description: `Get an incident role

Retrieve a single incident role from its ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await incidentSettingsGetIncidentRole(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
