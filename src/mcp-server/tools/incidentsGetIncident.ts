/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsGetIncident } from "../../funcs/incidentsGetIncident.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIncidentRequest$inboundSchema,
};

export const tool$incidentsGetIncident: ToolDefinition<typeof args> = {
  name: "incidents-get-incident",
  description: `Get an incident

Retrieve a single incident from its ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsGetIncident(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
