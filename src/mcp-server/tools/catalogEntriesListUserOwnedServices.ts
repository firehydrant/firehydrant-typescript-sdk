/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesListUserOwnedServices } from "../../funcs/catalogEntriesListUserOwnedServices.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUserOwnedServicesRequest$inboundSchema,
};

export const tool$catalogEntriesListUserOwnedServices: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-list-user-owned-services",
  description: `List services owned by a user's teams

Retrieves a list of services owned by the teams a user is on`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesListUserOwnedServices(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
