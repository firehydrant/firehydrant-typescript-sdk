/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingCreateSavedSearch } from "../../funcs/metricsReportingCreateSavedSearch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateSavedSearchRequest$inboundSchema,
};

export const tool$metricsReportingCreateSavedSearch: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-create-saved-search",
  description: `Create a saved search

Create a new saved search for a particular resource type`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsReportingCreateSavedSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
