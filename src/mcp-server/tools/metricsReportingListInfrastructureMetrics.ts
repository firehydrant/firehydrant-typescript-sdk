/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingListInfrastructureMetrics } from "../../funcs/metricsReportingListInfrastructureMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListInfrastructureMetricsRequest$inboundSchema,
};

export const tool$metricsReportingListInfrastructureMetrics: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-list-infrastructure-metrics",
  description: `Get metrics for a component

Return metrics for a specific component`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsReportingListInfrastructureMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
