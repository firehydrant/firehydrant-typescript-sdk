/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsListIncidentMilestones } from "../../funcs/incidentsListIncidentMilestones.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentMilestonesRequest$inboundSchema,
};

export const tool$incidentsListIncidentMilestones: ToolDefinition<typeof args> =
  {
    name: "incidents-list-incident-milestones",
    description: `List incident milestones

List times and durations for each milestone on an incident`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await incidentsListIncidentMilestones(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
