/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesDeleteNuncImage } from "../../funcs/statusPagesDeleteNuncImage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteNuncImageRequest$inboundSchema,
};

export const tool$statusPagesDeleteNuncImage: ToolDefinition<typeof args> = {
  name: "status-pages-delete-nunc-image",
  description: `Delete an image from a status page

Delete an image attached to a FireHydrant status page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesDeleteNuncImage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
