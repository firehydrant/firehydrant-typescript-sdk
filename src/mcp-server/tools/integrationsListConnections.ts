/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsListConnections } from "../../funcs/integrationsListConnections.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListConnectionsRequest$inboundSchema,
};

export const tool$integrationsListConnections: ToolDefinition<typeof args> = {
  name: "integrations-list-connections",
  description: `List integration connections

List available integration connections`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsListConnections(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
