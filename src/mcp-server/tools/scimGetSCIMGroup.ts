/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scimGetSCIMGroup } from "../../funcs/scimGetSCIMGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetScimGroupRequest$inboundSchema,
};

export const tool$scimGetSCIMGroup: ToolDefinition<typeof args> = {
  name: "scim-get-scim-group",
  description: `Get a SCIM group

SCIM endpoint that lists a Team (Colloquial for Group in the SCIM protocol)`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scimGetSCIMGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
