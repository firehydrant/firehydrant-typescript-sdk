/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesDeleteChangeEvent } from "../../funcs/changesDeleteChangeEvent.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteChangeEventRequest$inboundSchema,
};

export const tool$changesDeleteChangeEvent: ToolDefinition<typeof args> = {
  name: "changes-delete-change-event",
  description: `Delete a change event

Delete a change event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesDeleteChangeEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
