/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksCreateRunbook } from "../../funcs/runbooksCreateRunbook.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateRunbook$inboundSchema,
};

export const tool$runbooksCreateRunbook: ToolDefinition<typeof args> = {
  name: "runbooks-create-runbook",
  description: `Create a runbook

Create a new runbook for use with incidents.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runbooksCreateRunbook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
