/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksGetRunbookExecution } from "../../funcs/runbooksGetRunbookExecution.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRunbookExecutionRequest$inboundSchema,
};

export const tool$runbooksGetRunbookExecution: ToolDefinition<typeof args> = {
  name: "runbooks-get-runbook-execution",
  description: `Get a runbook execution

Retrieve a runbook execution by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runbooksGetRunbookExecution(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
