/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesGetServiceDependencies } from "../../funcs/catalogEntriesGetServiceDependencies.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetServiceDependenciesRequest$inboundSchema,
};

export const tool$catalogEntriesGetServiceDependencies: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-get-service-dependencies",
  description: `List dependencies for a service

Retrieves a service's dependencies`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesGetServiceDependencies(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
