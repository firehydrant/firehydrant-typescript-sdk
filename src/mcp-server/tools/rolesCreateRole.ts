/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { rolesCreateRole } from "../../funcs/rolesCreateRole.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateRole$inboundSchema,
};

export const tool$rolesCreateRole: ToolDefinition<typeof args> = {
  name: "roles-create-role",
  description: `Create a role

Create a new role`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await rolesCreateRole(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
