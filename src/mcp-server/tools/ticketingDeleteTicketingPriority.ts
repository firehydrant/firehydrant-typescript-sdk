/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingDeleteTicketingPriority } from "../../funcs/ticketingDeleteTicketingPriority.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTicketingPriorityRequest$inboundSchema,
};

export const tool$ticketingDeleteTicketingPriority: ToolDefinition<
  typeof args
> = {
  name: "ticketing-delete-ticketing-priority",
  description: `Delete a ticketing priority

Delete a single ticketing priority by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingDeleteTicketingPriority(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
