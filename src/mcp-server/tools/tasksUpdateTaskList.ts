/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksUpdateTaskList } from "../../funcs/tasksUpdateTaskList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTaskListRequest$inboundSchema,
};

export const tool$tasksUpdateTaskList: ToolDefinition<typeof args> = {
  name: "tasks-update-task-list",
  description: `Update a task list

Updates a task list's attributes and task list items`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksUpdateTaskList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
