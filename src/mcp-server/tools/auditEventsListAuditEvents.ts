/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { auditEventsListAuditEvents } from "../../funcs/auditEventsListAuditEvents.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAuditEventsRequest$inboundSchema,
};

export const tool$auditEventsListAuditEvents: ToolDefinition<typeof args> = {
  name: "audit-events-list-audit-events",
  description: `List audit events

List audit events`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await auditEventsListAuditEvents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
