/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsListSimilarIncidents } from "../../funcs/incidentsListSimilarIncidents.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListSimilarIncidentsRequest$inboundSchema,
};

export const tool$incidentsListSimilarIncidents: ToolDefinition<typeof args> = {
  name: "incidents-list-similar-incidents",
  description: `List similar incidents

Retrieve a list of similar incidents`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsListSimilarIncidents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
