/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsListIncidentRoles } from "../../funcs/incidentSettingsListIncidentRoles.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentRolesRequest$inboundSchema,
};

export const tool$incidentSettingsListIncidentRoles: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-list-incident-roles",
  description: `List incident roles

List all of the incident roles in the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsListIncidentRoles(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
