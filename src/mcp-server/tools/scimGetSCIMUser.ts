/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scimGetSCIMUser } from "../../funcs/scimGetSCIMUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetScimUserRequest$inboundSchema,
};

export const tool$scimGetSCIMUser: ToolDefinition<typeof args> = {
  name: "scim-get-scim-user",
  description: `Get a SCIM user

SCIM endpoint that lists a User`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scimGetSCIMUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
