/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesUpdateChangeIdentity } from "../../funcs/changesUpdateChangeIdentity.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateChangeIdentityRequest$inboundSchema,
};

export const tool$changesUpdateChangeIdentity: ToolDefinition<typeof args> = {
  name: "changes-update-change-identity",
  description: `Update an identity for a change entry

Update an identity for the change entry`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesUpdateChangeIdentity(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
