/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesUpdateChange } from "../../funcs/changesUpdateChange.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateChangeRequest$inboundSchema,
};

export const tool$changesUpdateChange: ToolDefinition<typeof args> = {
  name: "changes-update-change",
  description: `Update a change entry

Update a change entry`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesUpdateChange(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
