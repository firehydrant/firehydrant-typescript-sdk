/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsCreateSlackEmojiAction } from "../../funcs/integrationsCreateSlackEmojiAction.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateSlackEmojiActionRequest$inboundSchema,
};

export const tool$integrationsCreateSlackEmojiAction: ToolDefinition<
  typeof args
> = {
  name: "integrations-create-slack-emoji-action",
  description: `Create a new Slack emoji action

Creates a new Slack emoji action`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsCreateSlackEmojiAction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
