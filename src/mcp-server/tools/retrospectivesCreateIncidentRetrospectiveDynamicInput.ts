/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesCreateIncidentRetrospectiveDynamicInput } from "../../funcs/retrospectivesCreateIncidentRetrospectiveDynamicInput.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.CreateIncidentRetrospectiveDynamicInputRequest$inboundSchema,
};

export const tool$retrospectivesCreateIncidentRetrospectiveDynamicInput:
  ToolDefinition<typeof args> = {
    name: "retrospectives-create-incident-retrospective-dynamic-input",
    description:
      `Add a new dynamic input field to a retrospective's dynamic input group field

Add a new dynamic input field to a dynamic input group`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await retrospectivesCreateIncidentRetrospectiveDynamicInput(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
