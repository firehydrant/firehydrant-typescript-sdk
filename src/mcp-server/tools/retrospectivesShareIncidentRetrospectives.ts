/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesShareIncidentRetrospectives } from "../../funcs/retrospectivesShareIncidentRetrospectives.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ShareIncidentRetrospectivesRequest$inboundSchema,
};

export const tool$retrospectivesShareIncidentRetrospectives: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-share-incident-retrospectives",
  description: `Share an incident's retrospective

Share incident retrospectives with users or teams`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesShareIncidentRetrospectives(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
