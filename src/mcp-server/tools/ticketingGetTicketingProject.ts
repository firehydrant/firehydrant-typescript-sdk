/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingGetTicketingProject } from "../../funcs/ticketingGetTicketingProject.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTicketingProjectRequest$inboundSchema,
};

export const tool$ticketingGetTicketingProject: ToolDefinition<typeof args> = {
  name: "ticketing-get-ticketing-project",
  description: `Get a ticketing project

Retrieve a single ticketing project by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingGetTicketingProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
