/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsUpdateAuthedProvider } from "../../funcs/integrationsUpdateAuthedProvider.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAuthedProviderRequest$inboundSchema,
};

export const tool$integrationsUpdateAuthedProvider: ToolDefinition<
  typeof args
> = {
  name: "integrations-update-authed-provider",
  description: `Get an authed provider

Retrieve a single authed provider`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsUpdateAuthedProvider(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
