/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsListIncidentConferenceBridges } from "../../funcs/incidentsListIncidentConferenceBridges.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentConferenceBridgesRequest$inboundSchema,
};

export const tool$incidentsListIncidentConferenceBridges: ToolDefinition<
  typeof args
> = {
  name: "incidents-list-incident-conference-bridges",
  description: `Retrieve all conference bridges for an incident

Retrieve all conference bridges for an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsListIncidentConferenceBridges(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
