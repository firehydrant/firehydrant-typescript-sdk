/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsDeleteIncidentImpact } from "../../funcs/incidentsDeleteIncidentImpact.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteIncidentImpactRequest$inboundSchema,
};

export const tool$incidentsDeleteIncidentImpact: ToolDefinition<typeof args> = {
  name: "incidents-delete-incident-impact",
  description: `Remove impacted infrastructure from an incident

Remove impacted infrastructure from an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsDeleteIncidentImpact(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
