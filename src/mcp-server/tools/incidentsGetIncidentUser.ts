/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsGetIncidentUser } from "../../funcs/incidentsGetIncidentUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIncidentUserRequest$inboundSchema,
};

export const tool$incidentsGetIncidentUser: ToolDefinition<typeof args> = {
  name: "incidents-get-incident-user",
  description: `Get the current user's incident role

Retrieve a user with current roles for an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsGetIncidentUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
