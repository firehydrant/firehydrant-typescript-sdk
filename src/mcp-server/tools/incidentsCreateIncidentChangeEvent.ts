/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsCreateIncidentChangeEvent } from "../../funcs/incidentsCreateIncidentChangeEvent.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateIncidentChangeEventRequest$inboundSchema,
};

export const tool$incidentsCreateIncidentChangeEvent: ToolDefinition<
  typeof args
> = {
  name: "incidents-create-incident-change-event",
  description: `Add a related change to an incident

Add a related change to an incident. Changes added to an incident can be causes, fixes, or suspects. To remove a change from an incident, the type field should be set to dismissed.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsCreateIncidentChangeEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
