/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesCreateNuncConnection } from "../../funcs/statusPagesCreateNuncConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateNuncConnectionRequest$inboundSchema,
};

export const tool$statusPagesCreateNuncConnection: ToolDefinition<typeof args> =
  {
    name: "status-pages-create-nunc-connection",
    description: `Create a status page

Create a new FireHydrant hosted status page for customer facing statuses.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await statusPagesCreateNuncConnection(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
