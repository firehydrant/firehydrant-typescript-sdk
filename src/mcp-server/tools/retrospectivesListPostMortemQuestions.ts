/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesListPostMortemQuestions } from "../../funcs/retrospectivesListPostMortemQuestions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPostMortemQuestionsRequest$inboundSchema,
};

export const tool$retrospectivesListPostMortemQuestions: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-list-post-mortem-questions",
  description: `List retrospective questions

List the questions configured to be provided and filled out on each retrospective report.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesListPostMortemQuestions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
