/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesListChangeIdentities } from "../../funcs/changesListChangeIdentities.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListChangeIdentitiesRequest$inboundSchema,
};

export const tool$changesListChangeIdentities: ToolDefinition<typeof args> = {
  name: "changes-list-change-identities",
  description: `List identities for a change entry

Retrieve all identities for the change entry`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesListChangeIdentities(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
