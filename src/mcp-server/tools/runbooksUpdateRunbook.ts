/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksUpdateRunbook } from "../../funcs/runbooksUpdateRunbook.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateRunbookRequest$inboundSchema,
};

export const tool$runbooksUpdateRunbook: ToolDefinition<typeof args> = {
  name: "runbooks-update-runbook",
  description: `Update a runbook

Update a runbook and any attachment rules associated with it. This endpoint is used to configure nearly everything
about a runbook, including but not limited to the steps, environments, attachment rules, and severities.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runbooksUpdateRunbook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
