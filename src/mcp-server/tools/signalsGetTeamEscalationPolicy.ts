/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetTeamEscalationPolicy } from "../../funcs/signalsGetTeamEscalationPolicy.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTeamEscalationPolicyRequest$inboundSchema,
};

export const tool$signalsGetTeamEscalationPolicy: ToolDefinition<typeof args> =
  {
    name: "signals-get-team-escalation-policy",
    description: `Get an escalation policy for a team

Get a Signals escalation policy by ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsGetTeamEscalationPolicy(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
