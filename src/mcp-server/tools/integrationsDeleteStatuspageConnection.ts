/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsDeleteStatuspageConnection } from "../../funcs/integrationsDeleteStatuspageConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteStatuspageConnectionRequest$inboundSchema,
};

export const tool$integrationsDeleteStatuspageConnection: ToolDefinition<
  typeof args
> = {
  name: "integrations-delete-statuspage-connection",
  description: `Delete a Statuspage connection

Deletes the given Statuspage integration connection.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsDeleteStatuspageConnection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
