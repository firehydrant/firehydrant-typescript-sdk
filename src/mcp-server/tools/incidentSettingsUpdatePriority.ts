/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsUpdatePriority } from "../../funcs/incidentSettingsUpdatePriority.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdatePriorityRequest$inboundSchema,
};

export const tool$incidentSettingsUpdatePriority: ToolDefinition<typeof args> =
  {
    name: "incident-settings-update-priority",
    description: `Update a priority

Update a specific priority`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await incidentSettingsUpdatePriority(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
