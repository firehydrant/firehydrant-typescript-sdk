/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesListServices } from "../../funcs/catalogEntriesListServices.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListServicesRequest$inboundSchema,
};

export const tool$catalogEntriesListServices: ToolDefinition<typeof args> = {
  name: "catalog-entries-list-services",
  description: `List services

List all of the services that have been added to the organization.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesListServices(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
