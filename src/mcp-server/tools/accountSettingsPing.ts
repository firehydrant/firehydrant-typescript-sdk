/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountSettingsPing } from "../../funcs/accountSettingsPing.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$accountSettingsPing: ToolDefinition = {
  name: "account-settings-ping",
  description: `Check API connectivity

Simple endpoint to verify your API connection is working`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await accountSettingsPing(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
