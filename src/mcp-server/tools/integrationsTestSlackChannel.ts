/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsTestSlackChannel } from "../../funcs/integrationsTestSlackChannel.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.TestSlackChannelRequest$inboundSchema,
};

export const tool$integrationsTestSlackChannel: ToolDefinition<typeof args> = {
  name: "integrations-test-slack-channel",
  description: `Test a Slack channel

Test the ability of the FireHydrant bot to operate in a Slack channel`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsTestSlackChannel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
