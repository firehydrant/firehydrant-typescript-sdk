/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesCreateServiceChecklistResponse } from "../../funcs/catalogEntriesCreateServiceChecklistResponse.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateServiceChecklistResponseRequest$inboundSchema,
};

export const tool$catalogEntriesCreateServiceChecklistResponse: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-create-service-checklist-response",
  description: `Record a response for a checklist item

Creates a response for a checklist item`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await catalogEntriesCreateServiceChecklistResponse(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
