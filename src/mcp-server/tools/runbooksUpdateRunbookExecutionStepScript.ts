/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksUpdateRunbookExecutionStepScript } from "../../funcs/runbooksUpdateRunbookExecutionStepScript.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateRunbookExecutionStepScriptRequest$inboundSchema,
};

export const tool$runbooksUpdateRunbookExecutionStepScript: ToolDefinition<
  typeof args
> = {
  name: "runbooks-update-runbook-execution-step-script",
  description: `Update a script step's execution status

Updates the execution's step.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runbooksUpdateRunbookExecutionStepScript(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
