/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsListCustomFieldSelectOptions } from "../../funcs/incidentSettingsListCustomFieldSelectOptions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCustomFieldSelectOptionsRequest$inboundSchema,
};

export const tool$incidentSettingsListCustomFieldSelectOptions: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-list-custom-field-select-options",
  description: `Get available values for a custom field

Get the permissible values for the a currently active custom select or multi-select field.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await incidentSettingsListCustomFieldSelectOptions(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
