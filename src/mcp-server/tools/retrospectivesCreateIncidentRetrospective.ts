/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesCreateIncidentRetrospective } from "../../funcs/retrospectivesCreateIncidentRetrospective.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateIncidentRetrospectiveRequest$inboundSchema,
};

export const tool$retrospectivesCreateIncidentRetrospective: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-create-incident-retrospective",
  description: `Create a new retrospective on the incident using the template

Create a new retrospective for an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesCreateIncidentRetrospective(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
