/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksGetRunbookExecutionStepScript } from "../../funcs/runbooksGetRunbookExecutionStepScript.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRunbookExecutionStepScriptRequest$inboundSchema,
};

export const tool$runbooksGetRunbookExecutionStepScript: ToolDefinition<
  typeof args
> = {
  name: "runbooks-get-runbook-execution-step-script",
  description: `Get a step's bash script

Retrieves the bash script from a "script" step.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runbooksGetRunbookExecutionStepScript(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
