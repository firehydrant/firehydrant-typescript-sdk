/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsCreateTeamSignalRule } from "../../funcs/signalsCreateTeamSignalRule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTeamSignalRuleRequest$inboundSchema,
};

export const tool$signalsCreateTeamSignalRule: ToolDefinition<typeof args> = {
  name: "signals-create-team-signal-rule",
  description: `Create a Signals rule

Create a Signals rule for a team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsCreateTeamSignalRule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
