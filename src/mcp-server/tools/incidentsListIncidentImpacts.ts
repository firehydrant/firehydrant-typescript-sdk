/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsListIncidentImpacts } from "../../funcs/incidentsListIncidentImpacts.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentImpactsRequest$inboundSchema,
};

export const tool$incidentsListIncidentImpacts: ToolDefinition<typeof args> = {
  name: "incidents-list-incident-impacts",
  description: `List impacted infrastructure for an incident

List impacted infrastructure on an incident by specifying type`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsListIncidentImpacts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
