/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsCreateCustomFieldDefinition } from "../../funcs/incidentSettingsCreateCustomFieldDefinition.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateCustomFieldDefinition$inboundSchema,
};

export const tool$incidentSettingsCreateCustomFieldDefinition: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-create-custom-field-definition",
  description: `Create a custom field definition

Create a new custom field definition`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsCreateCustomFieldDefinition(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
