/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsListOrganizationOnCallSchedules } from "../../funcs/signalsListOrganizationOnCallSchedules.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListOrganizationOnCallSchedulesRequest$inboundSchema,
};

export const tool$signalsListOrganizationOnCallSchedules: ToolDefinition<
  typeof args
> = {
  name: "signals-list-organization-on-call-schedules",
  description: `List on-call schedules

List all Signals on-call schedules for the entire organization.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsListOrganizationOnCallSchedules(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
