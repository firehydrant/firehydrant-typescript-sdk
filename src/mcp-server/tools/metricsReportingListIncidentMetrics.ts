/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingListIncidentMetrics } from "../../funcs/metricsReportingListIncidentMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentMetricsRequest$inboundSchema,
};

export const tool$metricsReportingListIncidentMetrics: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-list-incident-metrics",
  description: `List incident metrics and analytics

Returns a report with time bucketed analytics data`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsReportingListIncidentMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
