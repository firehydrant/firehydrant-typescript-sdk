/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsDeleteIncidentRoleAssignment } from "../../funcs/incidentsDeleteIncidentRoleAssignment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteIncidentRoleAssignmentRequest$inboundSchema,
};

export const tool$incidentsDeleteIncidentRoleAssignment: ToolDefinition<
  typeof args
> = {
  name: "incidents-delete-incident-role-assignment",
  description: `Unassign a user from an incident

Unassign a role from a user`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsDeleteIncidentRoleAssignment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
