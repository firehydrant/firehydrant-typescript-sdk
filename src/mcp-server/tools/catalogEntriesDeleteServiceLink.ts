/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesDeleteServiceLink } from "../../funcs/catalogEntriesDeleteServiceLink.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteServiceLinkRequest$inboundSchema,
};

export const tool$catalogEntriesDeleteServiceLink: ToolDefinition<typeof args> =
  {
    name: "catalog-entries-delete-service-link",
    description: `Delete a service link

Deletes a service link from FireHydrant.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await catalogEntriesDeleteServiceLink(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
