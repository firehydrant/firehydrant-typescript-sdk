/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetSignalsHeartbeatEndpointConfiguration } from "../../funcs/signalsGetSignalsHeartbeatEndpointConfiguration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetSignalsHeartbeatEndpointConfigurationRequest$inboundSchema,
};

export const tool$signalsGetSignalsHeartbeatEndpointConfiguration:
  ToolDefinition<typeof args> = {
    name: "signals-get-signals-heartbeat-endpoint-configuration",
    description: `Get a heartbeat endpoint configuration

Retrieve a single heartbeat endpoint configuration`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await signalsGetSignalsHeartbeatEndpointConfiguration(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
