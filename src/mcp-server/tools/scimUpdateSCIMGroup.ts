/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scimUpdateSCIMGroup } from "../../funcs/scimUpdateSCIMGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateScimGroupRequest$inboundSchema,
};

export const tool$scimUpdateSCIMGroup: ToolDefinition<typeof args> = {
  name: "scim-update-scim-group",
  description: `Update a SCIM group and assign members

SCIM endpoint to update a Team (Colloquial for Group in the SCIM protocol). Any members defined in the payload will be assigned to the team with no defined role, any missing members will be removed from the team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scimUpdateSCIMGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
