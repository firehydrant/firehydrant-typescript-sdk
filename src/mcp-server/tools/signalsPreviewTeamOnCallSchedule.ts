/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsPreviewTeamOnCallSchedule } from "../../funcs/signalsPreviewTeamOnCallSchedule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PreviewTeamOnCallScheduleRequest$inboundSchema,
};

export const tool$signalsPreviewTeamOnCallSchedule: ToolDefinition<
  typeof args
> = {
  name: "signals-preview-team-on-call-schedule",
  description: `Preview a new on-call schedule for a team

Preview a new on-call schedule based on the provided rotations, allowing you to see how the schedule will look before saving it.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsPreviewTeamOnCallSchedule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
