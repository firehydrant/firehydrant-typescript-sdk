/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsDeleteTranscriptEntry } from "../../funcs/incidentsDeleteTranscriptEntry.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTranscriptEntryRequest$inboundSchema,
};

export const tool$incidentsDeleteTranscriptEntry: ToolDefinition<typeof args> =
  {
    name: "incidents-delete-transcript-entry",
    description: `Delete a transcript from an incident

Delete a transcript from an incident`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await incidentsDeleteTranscriptEntry(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
