/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesExportIncidentRetrospectivesMarkdown } from "../../funcs/retrospectivesExportIncidentRetrospectivesMarkdown.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ExportIncidentRetrospectivesMarkdownRequest$inboundSchema,
};

export const tool$retrospectivesExportIncidentRetrospectivesMarkdown:
  ToolDefinition<typeof args> = {
    name: "retrospectives-export-incident-retrospectives-markdown",
    description: `Export an incident's retrospective(s) as markdown

Export incident's retrospective(s) as markdown`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await retrospectivesExportIncidentRetrospectivesMarkdown(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
