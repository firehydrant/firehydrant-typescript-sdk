/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationsUpdateComment } from "../../funcs/conversationsUpdateComment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCommentRequest$inboundSchema,
};

export const tool$conversationsUpdateComment: ToolDefinition<typeof args> = {
  name: "conversations-update-comment",
  description: `Update a conversation comment

ALPHA - Update a comment's attributes`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await conversationsUpdateComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
