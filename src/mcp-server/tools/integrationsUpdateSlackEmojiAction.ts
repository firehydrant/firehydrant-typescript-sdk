/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsUpdateSlackEmojiAction } from "../../funcs/integrationsUpdateSlackEmojiAction.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSlackEmojiActionRequest$inboundSchema,
};

export const tool$integrationsUpdateSlackEmojiAction: ToolDefinition<
  typeof args
> = {
  name: "integrations-update-slack-emoji-action",
  description: `Update a Slack emoji action

Updates a Slack emoji action`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsUpdateSlackEmojiAction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
