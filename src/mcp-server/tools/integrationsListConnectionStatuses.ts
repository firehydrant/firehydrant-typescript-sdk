/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsListConnectionStatuses } from "../../funcs/integrationsListConnectionStatuses.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$integrationsListConnectionStatuses: ToolDefinition = {
  name: "integrations-list-connection-statuses",
  description: `Get integration connection status

Retrieve overall integration connection status`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await integrationsListConnectionStatuses(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
