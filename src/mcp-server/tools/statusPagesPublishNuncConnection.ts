/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesPublishNuncConnection } from "../../funcs/statusPagesPublishNuncConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PublishNuncConnectionRequest$inboundSchema,
};

export const tool$statusPagesPublishNuncConnection: ToolDefinition<
  typeof args
> = {
  name: "status-pages-publish-nunc-connection",
  description: `Publish a status page

Publish a FireHydrant hosted status page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesPublishNuncConnection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
