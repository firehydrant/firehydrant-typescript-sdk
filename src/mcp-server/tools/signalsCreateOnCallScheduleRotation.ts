/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsCreateOnCallScheduleRotation } from "../../funcs/signalsCreateOnCallScheduleRotation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateOnCallScheduleRotationRequest$inboundSchema,
};

export const tool$signalsCreateOnCallScheduleRotation: ToolDefinition<
  typeof args
> = {
  name: "signals-create-on-call-schedule-rotation",
  description: `Create a new on-call rotation

Add a new rotation to an existing on-call schedule`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsCreateOnCallScheduleRotation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
