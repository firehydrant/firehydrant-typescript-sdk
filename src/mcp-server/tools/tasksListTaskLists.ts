/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksListTaskLists } from "../../funcs/tasksListTaskLists.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTaskListsRequest$inboundSchema,
};

export const tool$tasksListTaskLists: ToolDefinition<typeof args> = {
  name: "tasks-list-task-lists",
  description: `List task lists

Lists all task lists for your organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksListTaskLists(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
