/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsSearchConfluenceSpaces } from "../../funcs/integrationsSearchConfluenceSpaces.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SearchConfluenceSpacesRequest$inboundSchema,
};

export const tool$integrationsSearchConfluenceSpaces: ToolDefinition<
  typeof args
> = {
  name: "integrations-search-confluence-spaces",
  description: `List Confluence spaces

Lists available space keys for the Confluence integration connection.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsSearchConfluenceSpaces(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
