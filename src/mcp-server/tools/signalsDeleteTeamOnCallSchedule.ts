/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsDeleteTeamOnCallSchedule } from "../../funcs/signalsDeleteTeamOnCallSchedule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTeamOnCallScheduleRequest$inboundSchema,
};

export const tool$signalsDeleteTeamOnCallSchedule: ToolDefinition<typeof args> =
  {
    name: "signals-delete-team-on-call-schedule",
    description: `Delete an on-call schedule for a team

Delete a Signals on-call schedule by ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsDeleteTeamOnCallSchedule(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
