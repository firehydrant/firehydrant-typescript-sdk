/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { auditEventsGetAuditEvent } from "../../funcs/auditEventsGetAuditEvent.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAuditEventRequest$inboundSchema,
};

export const tool$auditEventsGetAuditEvent: ToolDefinition<typeof args> = {
  name: "audit-events-get-audit-event",
  description: `Get a single audit event

Get a single audit event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await auditEventsGetAuditEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
