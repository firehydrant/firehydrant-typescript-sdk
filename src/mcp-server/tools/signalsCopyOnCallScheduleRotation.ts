/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsCopyOnCallScheduleRotation } from "../../funcs/signalsCopyOnCallScheduleRotation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CopyOnCallScheduleRotationRequest$inboundSchema,
};

export const tool$signalsCopyOnCallScheduleRotation: ToolDefinition<
  typeof args
> = {
  name: "signals-copy-on-call-schedule-rotation",
  description: `Copy an on-call schedule's rotation

Copy an on-call rotation into a different schedule, allowing you to merge them together safely.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsCopyOnCallScheduleRotation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
