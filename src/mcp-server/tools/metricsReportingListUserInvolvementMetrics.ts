/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingListUserInvolvementMetrics } from "../../funcs/metricsReportingListUserInvolvementMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUserInvolvementMetricsRequest$inboundSchema,
};

export const tool$metricsReportingListUserInvolvementMetrics: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-list-user-involvement-metrics",
  description: `List user metrics

Returns a report with time bucketed analytics data`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsReportingListUserInvolvementMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
