/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsListIncidentStatusPages } from "../../funcs/incidentsListIncidentStatusPages.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentStatusPagesRequest$inboundSchema,
};

export const tool$incidentsListIncidentStatusPages: ToolDefinition<
  typeof args
> = {
  name: "incidents-list-incident-status-pages",
  description: `List status pages for an incident

List status pages that are attached to an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsListIncidentStatusPages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
