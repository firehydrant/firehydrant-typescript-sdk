/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksUpdateRunbookExecutionStep } from "../../funcs/runbooksUpdateRunbookExecutionStep.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateRunbookExecutionStepRequest$inboundSchema,
};

export const tool$runbooksUpdateRunbookExecutionStep: ToolDefinition<
  typeof args
> = {
  name: "runbooks-update-runbook-execution-step",
  description: `Update a runbook step execution

Updates a runbook step execution, especially for changing the state of a step execution.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runbooksUpdateRunbookExecutionStep(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
