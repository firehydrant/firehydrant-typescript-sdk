/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksDeleteChecklistTemplate } from "../../funcs/tasksDeleteChecklistTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteChecklistTemplateRequest$inboundSchema,
};

export const tool$tasksDeleteChecklistTemplate: ToolDefinition<typeof args> = {
  name: "tasks-delete-checklist-template",
  description: `Archive a checklist template

Archive a checklist template`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksDeleteChecklistTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
