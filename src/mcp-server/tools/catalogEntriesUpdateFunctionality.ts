/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesUpdateFunctionality } from "../../funcs/catalogEntriesUpdateFunctionality.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateFunctionalityRequest$inboundSchema,
};

export const tool$catalogEntriesUpdateFunctionality: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-update-functionality",
  description: `Update a functionality

Update a functionalities attributes`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesUpdateFunctionality(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
