/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsUpdateSeverityMatrix } from "../../funcs/incidentSettingsUpdateSeverityMatrix.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.UpdateSeverityMatrix$inboundSchema,
};

export const tool$incidentSettingsUpdateSeverityMatrix: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-update-severity-matrix",
  description: `Update severity matrix

Update available severities and impacts in your organization's severity matrix.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsUpdateSeverityMatrix(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
