/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesCreateServiceLinks } from "../../funcs/catalogEntriesCreateServiceLinks.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateServiceLinks$inboundSchema,
};

export const tool$catalogEntriesCreateServiceLinks: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-create-service-links",
  description: `Create multiple services linked to external services

Creates a service with the appropriate integration for each external service ID passed`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesCreateServiceLinks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
