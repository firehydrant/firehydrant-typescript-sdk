/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingGetInboundFieldMap } from "../../funcs/ticketingGetInboundFieldMap.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetInboundFieldMapRequest$inboundSchema,
};

export const tool$ticketingGetInboundFieldMap: ToolDefinition<typeof args> = {
  name: "ticketing-get-inbound-field-map",
  description: `Get inbound field map for a ticketing project

Retrieve inbound field map for a ticketing project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingGetInboundFieldMap(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
