/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksListRunbookActions } from "../../funcs/runbooksListRunbookActions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRunbookActionsRequest$inboundSchema,
};

export const tool$runbooksListRunbookActions: ToolDefinition<typeof args> = {
  name: "runbooks-list-runbook-actions",
  description: `List runbook actions

List all runbook actions available through your connected integrations`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runbooksListRunbookActions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
