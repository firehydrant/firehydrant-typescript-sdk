/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { audiencesRestoreAudience } from "../../funcs/audiencesRestoreAudience.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RestoreAudienceRequest$inboundSchema,
};

export const tool$audiencesRestoreAudience: ToolDefinition<typeof args> = {
  name: "audiences-restore-audience",
  description: `Restore audience

Restore a previously archived audience`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await audiencesRestoreAudience(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
