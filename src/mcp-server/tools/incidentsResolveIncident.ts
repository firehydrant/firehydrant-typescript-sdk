/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsResolveIncident } from "../../funcs/incidentsResolveIncident.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ResolveIncidentRequest$inboundSchema,
};

export const tool$incidentsResolveIncident: ToolDefinition<typeof args> = {
  name: "incidents-resolve-incident",
  description: `Resolve an incident

Resolves a currently active incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsResolveIncident(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
