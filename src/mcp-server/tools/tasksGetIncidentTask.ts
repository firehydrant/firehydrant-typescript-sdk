/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksGetIncidentTask } from "../../funcs/tasksGetIncidentTask.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIncidentTaskRequest$inboundSchema,
};

export const tool$tasksGetIncidentTask: ToolDefinition<typeof args> = {
  name: "tasks-get-incident-task",
  description: `Get an incident task

Retrieve a single task for an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksGetIncidentTask(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
