/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsListSignalsWebhookTargets } from "../../funcs/signalsListSignalsWebhookTargets.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListSignalsWebhookTargetsRequest$inboundSchema,
};

export const tool$signalsListSignalsWebhookTargets: ToolDefinition<
  typeof args
> = {
  name: "signals-list-signals-webhook-targets",
  description: `List webhook targets

List all Signals webhook targets.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsListSignalsWebhookTargets(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
