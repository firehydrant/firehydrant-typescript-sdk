/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksListRunbookExecutions } from "../../funcs/runbooksListRunbookExecutions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRunbookExecutionsRequest$inboundSchema,
};

export const tool$runbooksListRunbookExecutions: ToolDefinition<typeof args> = {
  name: "runbooks-list-runbook-executions",
  description: `List runbook executions

List all runbook executions across all runbooks`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runbooksListRunbookExecutions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
