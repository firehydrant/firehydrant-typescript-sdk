/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesPublishPostMortemReport } from "../../funcs/retrospectivesPublishPostMortemReport.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PublishPostMortemReportRequest$inboundSchema,
};

export const tool$retrospectivesPublishPostMortemReport: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-publish-post-mortem-report",
  description: `Publish a retrospective report

Marks an incident retrospective as published and emails all of the participants in the report the summary`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesPublishPostMortemReport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
