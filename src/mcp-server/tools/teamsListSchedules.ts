/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsListSchedules } from "../../funcs/teamsListSchedules.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListSchedulesRequest$inboundSchema,
};

export const tool$teamsListSchedules: ToolDefinition<typeof args> = {
  name: "teams-list-schedules",
  description: `List schedules

List all known schedules in FireHydrant as pulled from external sources`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamsListSchedules(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
