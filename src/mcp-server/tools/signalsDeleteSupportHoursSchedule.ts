/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsDeleteSupportHoursSchedule } from "../../funcs/signalsDeleteSupportHoursSchedule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSupportHoursScheduleRequest$inboundSchema,
};

export const tool$signalsDeleteSupportHoursSchedule: ToolDefinition<
  typeof args
> = {
  name: "signals-delete-support-hours-schedule",
  description: `Delete a specific support hours schedule

Delete a specific support hours schedule`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsDeleteSupportHoursSchedule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
