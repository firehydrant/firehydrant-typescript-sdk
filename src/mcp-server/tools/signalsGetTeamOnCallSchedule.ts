/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetTeamOnCallSchedule } from "../../funcs/signalsGetTeamOnCallSchedule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTeamOnCallScheduleRequest$inboundSchema,
};

export const tool$signalsGetTeamOnCallSchedule: ToolDefinition<typeof args> = {
  name: "signals-get-team-on-call-schedule",
  description: `Get an on-call schedule for a team

Get a Signals on-call schedule by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsGetTeamOnCallSchedule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
