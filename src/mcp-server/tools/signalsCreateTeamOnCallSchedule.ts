/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsCreateTeamOnCallSchedule } from "../../funcs/signalsCreateTeamOnCallSchedule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTeamOnCallScheduleRequest$inboundSchema,
};

export const tool$signalsCreateTeamOnCallSchedule: ToolDefinition<typeof args> =
  {
    name: "signals-create-team-on-call-schedule",
    description: `Create an on-call schedule for a team

Create a Signals on-call schedule for a team.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsCreateTeamOnCallSchedule(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
