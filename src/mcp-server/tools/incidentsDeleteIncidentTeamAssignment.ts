/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsDeleteIncidentTeamAssignment } from "../../funcs/incidentsDeleteIncidentTeamAssignment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteIncidentTeamAssignmentRequest$inboundSchema,
};

export const tool$incidentsDeleteIncidentTeamAssignment: ToolDefinition<
  typeof args
> = {
  name: "incidents-delete-incident-team-assignment",
  description: `Unassign a team from an incident

Unassign a team from an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsDeleteIncidentTeamAssignment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
