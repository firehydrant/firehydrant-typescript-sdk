/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsDeleteIncidentType } from "../../funcs/incidentSettingsDeleteIncidentType.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteIncidentTypeRequest$inboundSchema,
};

export const tool$incidentSettingsDeleteIncidentType: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-delete-incident-type",
  description: `Archive an incident type

Archives an incident type which will hide it from lists and metrics`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsDeleteIncidentType(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
