/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsListAwsConnections } from "../../funcs/integrationsListAwsConnections.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAwsConnectionsRequest$inboundSchema,
};

export const tool$integrationsListAwsConnections: ToolDefinition<typeof args> =
  {
    name: "integrations-list-aws-connections",
    description: `List AWS connections

Lists the available and configured AWS integration connections for the authenticated organization.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await integrationsListAwsConnections(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
