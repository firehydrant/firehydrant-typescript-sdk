/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsGetPriority } from "../../funcs/incidentSettingsGetPriority.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPriorityRequest$inboundSchema,
};

export const tool$incidentSettingsGetPriority: ToolDefinition<typeof args> = {
  name: "incident-settings-get-priority",
  description: `Get a priority

Retrieve a specific priority`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsGetPriority(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
