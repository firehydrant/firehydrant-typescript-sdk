/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesCreateChange } from "../../funcs/changesCreateChange.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateChange$inboundSchema,
};

export const tool$changesCreateChange: ToolDefinition<typeof args> = {
  name: "changes-create-change",
  description: `Create a new change entry

Create a new change entry`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesCreateChange(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
