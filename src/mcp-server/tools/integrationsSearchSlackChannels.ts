/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsSearchSlackChannels } from "../../funcs/integrationsSearchSlackChannels.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SearchSlackChannelsRequest$inboundSchema,
};

export const tool$integrationsSearchSlackChannels: ToolDefinition<typeof args> =
  {
    name: "integrations-search-slack-channels",
    description: `List Slack channels

List or search for known Slack channels that the FireHydrant bot can access`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await integrationsSearchSlackChannels(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
