/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsListTeamEscalationPolicies } from "../../funcs/signalsListTeamEscalationPolicies.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTeamEscalationPoliciesRequest$inboundSchema,
};

export const tool$signalsListTeamEscalationPolicies: ToolDefinition<
  typeof args
> = {
  name: "signals-list-team-escalation-policies",
  description: `List escalation policies for a team

List all Signals escalation policies for a team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsListTeamEscalationPolicies(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
