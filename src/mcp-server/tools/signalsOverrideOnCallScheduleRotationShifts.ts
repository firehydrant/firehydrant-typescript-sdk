/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsOverrideOnCallScheduleRotationShifts } from "../../funcs/signalsOverrideOnCallScheduleRotationShifts.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.OverrideOnCallScheduleRotationShiftsRequest$inboundSchema,
};

export const tool$signalsOverrideOnCallScheduleRotationShifts: ToolDefinition<
  typeof args
> = {
  name: "signals-override-on-call-schedule-rotation-shifts",
  description: `Override one or more shifts in an on-call rotation

Create an override covering a specific time period in an on-call rotation, re-assigning that period to a specific user.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsOverrideOnCallScheduleRotationShifts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
