/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesCreateNuncLink } from "../../funcs/statusPagesCreateNuncLink.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateNuncLinkRequest$inboundSchema,
};

export const tool$statusPagesCreateNuncLink: ToolDefinition<typeof args> = {
  name: "status-pages-create-nunc-link",
  description: `Add link to a status page

Add a link to be displayed on a FireHydrant status page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesCreateNuncLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
