/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingCreateTicketingCustomDefinition } from "../../funcs/ticketingCreateTicketingCustomDefinition.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateTicketingCustomDefinition$inboundSchema,
};

export const tool$ticketingCreateTicketingCustomDefinition: ToolDefinition<
  typeof args
> = {
  name: "ticketing-create-ticketing-custom-definition",
  description: `Create a ticketing custom field

Creates a ticketing custom field for the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingCreateTicketingCustomDefinition(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
