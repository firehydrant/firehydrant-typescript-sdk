/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountSettingsUpdateAiPreferences } from "../../funcs/accountSettingsUpdateAiPreferences.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAiPreferencesRequest$inboundSchema.optional(),
};

export const tool$accountSettingsUpdateAiPreferences: ToolDefinition<
  typeof args
> = {
  name: "account-settings-update-ai-preferences",
  description: `Update AI preferences

Updates the AI preferences`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountSettingsUpdateAiPreferences(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
