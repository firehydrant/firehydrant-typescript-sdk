/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksGetRunbookActionFieldOptions } from "../../funcs/runbooksGetRunbookActionFieldOptions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRunbookActionFieldOptionsRequest$inboundSchema,
};

export const tool$runbooksGetRunbookActionFieldOptions: ToolDefinition<
  typeof args
> = {
  name: "runbooks-get-runbook-action-field-options",
  description: `List select options for a runbook integration action field

List select options for a runbook integration action field`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runbooksGetRunbookActionFieldOptions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
