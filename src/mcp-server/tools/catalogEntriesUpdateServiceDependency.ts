/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesUpdateServiceDependency } from "../../funcs/catalogEntriesUpdateServiceDependency.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateServiceDependencyRequest$inboundSchema,
};

export const tool$catalogEntriesUpdateServiceDependency: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-update-service-dependency",
  description: `Update a service dependency

Update the notes of the service dependency`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesUpdateServiceDependency(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
