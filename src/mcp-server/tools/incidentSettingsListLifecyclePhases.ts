/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsListLifecyclePhases } from "../../funcs/incidentSettingsListLifecyclePhases.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$incidentSettingsListLifecyclePhases: ToolDefinition = {
  name: "incident-settings-list-lifecycle-phases",
  description: `List phases and milestones

List all of the lifecycle phases and milestones in the organization`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await incidentSettingsListLifecyclePhases(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
