/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { permissionsListTeamPermissions } from "../../funcs/permissionsListTeamPermissions.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$permissionsListTeamPermissions: ToolDefinition = {
  name: "permissions-list-team-permissions",
  description: `Get all permissions for a team

Get all permissions for a team`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await permissionsListTeamPermissions(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
