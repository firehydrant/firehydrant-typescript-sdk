/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingCreateTicketingPriority } from "../../funcs/ticketingCreateTicketingPriority.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateTicketingPriority$inboundSchema,
};

export const tool$ticketingCreateTicketingPriority: ToolDefinition<
  typeof args
> = {
  name: "ticketing-create-ticketing-priority",
  description: `Create a ticketing priority

Create a single ticketing priority`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingCreateTicketingPriority(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
