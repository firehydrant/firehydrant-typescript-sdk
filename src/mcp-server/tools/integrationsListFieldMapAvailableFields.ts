/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsListFieldMapAvailableFields } from "../../funcs/integrationsListFieldMapAvailableFields.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListFieldMapAvailableFieldsRequest$inboundSchema,
};

export const tool$integrationsListFieldMapAvailableFields: ToolDefinition<
  typeof args
> = {
  name: "integrations-list-field-map-available-fields",
  description: `List available fields for field mapping

Get a description of the fields to which data can be mapped`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsListFieldMapAvailableFields(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
