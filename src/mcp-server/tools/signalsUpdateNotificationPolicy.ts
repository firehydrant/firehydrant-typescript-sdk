/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsUpdateNotificationPolicy } from "../../funcs/signalsUpdateNotificationPolicy.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateNotificationPolicyRequest$inboundSchema,
};

export const tool$signalsUpdateNotificationPolicy: ToolDefinition<typeof args> =
  {
    name: "signals-update-notification-policy",
    description: `Update a notification policy

Update a Signals notification policy by ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsUpdateNotificationPolicy(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
