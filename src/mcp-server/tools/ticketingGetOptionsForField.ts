/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingGetOptionsForField } from "../../funcs/ticketingGetOptionsForField.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOptionsForFieldRequest$inboundSchema,
};

export const tool$ticketingGetOptionsForField: ToolDefinition<typeof args> = {
  name: "ticketing-get-options-for-field",
  description: `List a field's configuration options for a ticketing project

List a field's configuration options for a ticketing project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingGetOptionsForField(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
