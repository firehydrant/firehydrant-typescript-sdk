/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesUpdateNuncLink } from "../../funcs/statusPagesUpdateNuncLink.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateNuncLinkRequest$inboundSchema,
};

export const tool$statusPagesUpdateNuncLink: ToolDefinition<typeof args> = {
  name: "status-pages-update-nunc-link",
  description: `Update a status page link

Update a link to be displayed on a FireHydrant status page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesUpdateNuncLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
