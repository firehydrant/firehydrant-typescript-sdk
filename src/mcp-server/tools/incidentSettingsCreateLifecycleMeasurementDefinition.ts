/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsCreateLifecycleMeasurementDefinition } from "../../funcs/incidentSettingsCreateLifecycleMeasurementDefinition.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLifecycleMeasurementDefinitionRequest$inboundSchema,
};

export const tool$incidentSettingsCreateLifecycleMeasurementDefinition:
  ToolDefinition<typeof args> = {
    name: "incident-settings-create-lifecycle-measurement-definition",
    description: `Create a measurement definition

Create a new measurement definition`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await incidentSettingsCreateLifecycleMeasurementDefinition(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
