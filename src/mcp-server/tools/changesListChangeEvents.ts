/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesListChangeEvents } from "../../funcs/changesListChangeEvents.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListChangeEventsRequest$inboundSchema,
};

export const tool$changesListChangeEvents: ToolDefinition<typeof args> = {
  name: "changes-list-change-events",
  description: `List change events

List change events for the organization. Note: Not all information is included on a change event like attachments and related changes. You must fetch a change event separately to retrieve all of the information about it`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesListChangeEvents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
