/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksListIncidentTasks } from "../../funcs/tasksListIncidentTasks.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentTasksRequest$inboundSchema,
};

export const tool$tasksListIncidentTasks: ToolDefinition<typeof args> = {
  name: "tasks-list-incident-tasks",
  description: `List tasks for an incident

Retrieve a list of all tasks for a specific incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksListIncidentTasks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
