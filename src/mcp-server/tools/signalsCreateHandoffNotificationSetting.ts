/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsCreateHandoffNotificationSetting } from "../../funcs/signalsCreateHandoffNotificationSetting.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateHandoffNotificationSettingRequest$inboundSchema,
};

export const tool$signalsCreateHandoffNotificationSetting: ToolDefinition<
  typeof args
> = {
  name: "signals-create-handoff-notification-setting",
  description: `Create a notification policy

Create a Signals notification policy.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsCreateHandoffNotificationSetting(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
