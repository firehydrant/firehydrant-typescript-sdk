/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksListRunbookAudits } from "../../funcs/runbooksListRunbookAudits.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRunbookAuditsRequest$inboundSchema,
};

export const tool$runbooksListRunbookAudits: ToolDefinition<typeof args> = {
  name: "runbooks-list-runbook-audits",
  description: `List runbook audits

Please contact support to enable audit logging for your account.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runbooksListRunbookAudits(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
