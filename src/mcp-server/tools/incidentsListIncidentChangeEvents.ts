/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsListIncidentChangeEvents } from "../../funcs/incidentsListIncidentChangeEvents.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentChangeEventsRequest$inboundSchema,
};

export const tool$incidentsListIncidentChangeEvents: ToolDefinition<
  typeof args
> = {
  name: "incidents-list-incident-change-events",
  description: `List related changes on an incident

List related changes that have been attached to an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsListIncidentChangeEvents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
