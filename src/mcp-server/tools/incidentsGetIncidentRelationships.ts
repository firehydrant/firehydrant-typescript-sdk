/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsGetIncidentRelationships } from "../../funcs/incidentsGetIncidentRelationships.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIncidentRelationshipsRequest$inboundSchema,
};

export const tool$incidentsGetIncidentRelationships: ToolDefinition<
  typeof args
> = {
  name: "incidents-get-incident-relationships",
  description: `List incident relationships

List any parent/child relationships for an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsGetIncidentRelationships(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
