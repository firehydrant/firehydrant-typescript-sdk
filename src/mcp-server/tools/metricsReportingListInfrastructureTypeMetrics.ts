/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingListInfrastructureTypeMetrics } from "../../funcs/metricsReportingListInfrastructureTypeMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListInfrastructureTypeMetricsRequest$inboundSchema,
};

export const tool$metricsReportingListInfrastructureTypeMetrics: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-list-infrastructure-type-metrics",
  description: `List metrics for a component type

Returns metrics for all components of a given type`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await metricsReportingListInfrastructureTypeMetrics(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
