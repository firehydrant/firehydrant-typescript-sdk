/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsListUserNotificationSettingsByUserId } from "../../funcs/signalsListUserNotificationSettingsByUserId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUserNotificationSettingsByUserIdRequest$inboundSchema,
};

export const tool$signalsListUserNotificationSettingsByUserId: ToolDefinition<
  typeof args
> = {
  name: "signals-list-user-notification-settings-by-user-id",
  description: `List notification settings for a user

List all Signals notification settings for a specific user. Requires an API key with PII access enabled.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsListUserNotificationSettingsByUserId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
