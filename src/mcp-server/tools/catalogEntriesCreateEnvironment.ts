/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesCreateEnvironment } from "../../funcs/catalogEntriesCreateEnvironment.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateEnvironment$inboundSchema,
};

export const tool$catalogEntriesCreateEnvironment: ToolDefinition<typeof args> =
  {
    name: "catalog-entries-create-environment",
    description: `Create an environment

Creates an environment for the organization`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await catalogEntriesCreateEnvironment(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
