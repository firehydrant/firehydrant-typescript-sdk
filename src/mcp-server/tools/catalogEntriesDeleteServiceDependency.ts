/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesDeleteServiceDependency } from "../../funcs/catalogEntriesDeleteServiceDependency.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteServiceDependencyRequest$inboundSchema,
};

export const tool$catalogEntriesDeleteServiceDependency: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-delete-service-dependency",
  description: `Delete a service dependency

Deletes a single service dependency`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesDeleteServiceDependency(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
