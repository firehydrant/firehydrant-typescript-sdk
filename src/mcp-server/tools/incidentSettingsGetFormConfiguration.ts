/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsGetFormConfiguration } from "../../funcs/incidentSettingsGetFormConfiguration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFormConfigurationRequest$inboundSchema,
};

export const tool$incidentSettingsGetFormConfiguration: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-get-form-configuration",
  description: `Get a form configuration

Get a form configuration`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsGetFormConfiguration(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
