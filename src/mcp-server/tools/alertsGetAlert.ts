/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { alertsGetAlert } from "../../funcs/alertsGetAlert.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAlertRequest$inboundSchema,
};

export const tool$alertsGetAlert: ToolDefinition<typeof args> = {
  name: "alerts-get-alert",
  description: `Get an alert

Retrieve a single alert`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await alertsGetAlert(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
