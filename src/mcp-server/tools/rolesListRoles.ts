/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { rolesListRoles } from "../../funcs/rolesListRoles.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRolesRequest$inboundSchema,
};

export const tool$rolesListRoles: ToolDefinition<typeof args> = {
  name: "roles-list-roles",
  description: `Get all roles

Get all roles in the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await rolesListRoles(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
