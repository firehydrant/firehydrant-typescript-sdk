/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsUpdateIncidentRole } from "../../funcs/incidentSettingsUpdateIncidentRole.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateIncidentRoleRequest$inboundSchema,
};

export const tool$incidentSettingsUpdateIncidentRole: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-update-incident-role",
  description: `Update an incident role

Update a single incident role from its ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsUpdateIncidentRole(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
