/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesUpdateEnvironment } from "../../funcs/catalogEntriesUpdateEnvironment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateEnvironmentRequest$inboundSchema,
};

export const tool$catalogEntriesUpdateEnvironment: ToolDefinition<typeof args> =
  {
    name: "catalog-entries-update-environment",
    description: `Update an environment

Update a environments attributes`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await catalogEntriesUpdateEnvironment(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
