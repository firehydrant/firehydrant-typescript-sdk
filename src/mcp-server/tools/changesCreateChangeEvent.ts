/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesCreateChangeEvent } from "../../funcs/changesCreateChangeEvent.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateChangeEvent$inboundSchema,
};

export const tool$changesCreateChangeEvent: ToolDefinition<typeof args> = {
  name: "changes-create-change-event",
  description: `Create a change event

Create a change event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesCreateChangeEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
