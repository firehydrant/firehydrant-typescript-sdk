/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesCreateNuncSubscription } from "../../funcs/statusPagesCreateNuncSubscription.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateNuncSubscription$inboundSchema,
};

export const tool$statusPagesCreateNuncSubscription: ToolDefinition<
  typeof args
> = {
  name: "status-pages-create-nunc-subscription",
  description: `Create a status page subscription

Subscribe to status page updates`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesCreateNuncSubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
