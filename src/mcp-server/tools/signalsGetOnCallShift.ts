/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetOnCallShift } from "../../funcs/signalsGetOnCallShift.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOnCallShiftRequest$inboundSchema,
};

export const tool$signalsGetOnCallShift: ToolDefinition<typeof args> = {
  name: "signals-get-on-call-shift",
  description: `Get an on-call shift for a team schedule

Get a Signals on-call shift by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsGetOnCallShift(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
