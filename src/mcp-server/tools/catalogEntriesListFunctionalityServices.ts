/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesListFunctionalityServices } from "../../funcs/catalogEntriesListFunctionalityServices.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListFunctionalityServicesRequest$inboundSchema,
};

export const tool$catalogEntriesListFunctionalityServices: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-list-functionality-services",
  description: `List services for a functionality

List services for a functionality`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesListFunctionalityServices(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
