/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsCreateNotificationPolicy } from "../../funcs/signalsCreateNotificationPolicy.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateNotificationPolicyRequest$inboundSchema,
};

export const tool$signalsCreateNotificationPolicy: ToolDefinition<typeof args> =
  {
    name: "signals-create-notification-policy",
    description: `Create a notification policy

Create a Signals notification policy.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsCreateNotificationPolicy(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
