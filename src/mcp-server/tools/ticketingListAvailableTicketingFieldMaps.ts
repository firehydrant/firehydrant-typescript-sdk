/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingListAvailableTicketingFieldMaps } from "../../funcs/ticketingListAvailableTicketingFieldMaps.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAvailableTicketingFieldMapsRequest$inboundSchema,
};

export const tool$ticketingListAvailableTicketingFieldMaps: ToolDefinition<
  typeof args
> = {
  name: "ticketing-list-available-ticketing-field-maps",
  description: `List available fields for ticket field mapping

Returns metadata for the fields that are available for field mapping.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingListAvailableTicketingFieldMaps(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
