/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksGetTaskList } from "../../funcs/tasksGetTaskList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTaskListRequest$inboundSchema,
};

export const tool$tasksGetTaskList: ToolDefinition<typeof args> = {
  name: "tasks-get-task-list",
  description: `Get a task list

Retrieves a single task list by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksGetTaskList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
