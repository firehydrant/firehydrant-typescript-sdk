/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesCreateService } from "../../funcs/catalogEntriesCreateService.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateService$inboundSchema,
};

export const tool$catalogEntriesCreateService: ToolDefinition<typeof args> = {
  name: "catalog-entries-create-service",
  description: `Create a service

Creates a service for the organization, you may also create or attach functionalities to the service on create.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesCreateService(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
