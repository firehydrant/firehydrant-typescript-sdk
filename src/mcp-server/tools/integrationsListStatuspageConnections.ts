/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsListStatuspageConnections } from "../../funcs/integrationsListStatuspageConnections.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListStatuspageConnectionsRequest$inboundSchema,
};

export const tool$integrationsListStatuspageConnections: ToolDefinition<
  typeof args
> = {
  name: "integrations-list-statuspage-connections",
  description: `List Statuspage connections

Lists the available and configured Statuspage integrations connections for the authenticated organization.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsListStatuspageConnections(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
