/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingGetTicketingFieldMap } from "../../funcs/ticketingGetTicketingFieldMap.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTicketingFieldMapRequest$inboundSchema,
};

export const tool$ticketingGetTicketingFieldMap: ToolDefinition<typeof args> = {
  name: "ticketing-get-ticketing-field-map",
  description: `Get a field map for a ticketing project

Retrieve field map for a ticketing project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingGetTicketingFieldMap(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
