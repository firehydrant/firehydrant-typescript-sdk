/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesListEnvironmentFunctionalities } from "../../funcs/catalogEntriesListEnvironmentFunctionalities.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListEnvironmentFunctionalitiesRequest$inboundSchema,
};

export const tool$catalogEntriesListEnvironmentFunctionalities: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-list-environment-functionalities",
  description: `List functionalities for an environment

List functionalities for an environment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await catalogEntriesListEnvironmentFunctionalities(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
