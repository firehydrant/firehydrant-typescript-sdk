/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsDeleteIncidentEvent } from "../../funcs/incidentsDeleteIncidentEvent.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteIncidentEventRequest$inboundSchema,
};

export const tool$incidentsDeleteIncidentEvent: ToolDefinition<typeof args> = {
  name: "incidents-delete-incident-event",
  description: `Delete an incident event

Delete an event for an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsDeleteIncidentEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
