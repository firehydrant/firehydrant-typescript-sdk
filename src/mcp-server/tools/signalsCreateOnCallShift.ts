/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsCreateOnCallShift } from "../../funcs/signalsCreateOnCallShift.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateOnCallShiftRequest$inboundSchema,
};

export const tool$signalsCreateOnCallShift: ToolDefinition<typeof args> = {
  name: "signals-create-on-call-shift",
  description: `Create a shift for an on-call schedule

Create a Signals on-call shift in a schedule.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsCreateOnCallShift(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
