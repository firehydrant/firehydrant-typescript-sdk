/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesListPostMortemReasons } from "../../funcs/retrospectivesListPostMortemReasons.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPostMortemReasonsRequest$inboundSchema,
};

export const tool$retrospectivesListPostMortemReasons: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-list-post-mortem-reasons",
  description: `List contributing factors for a retrospective report

List all contributing factors to an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesListPostMortemReasons(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
