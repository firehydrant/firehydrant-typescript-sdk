/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesExportIncidentRetrospectives } from "../../funcs/retrospectivesExportIncidentRetrospectives.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ExportIncidentRetrospectivesRequest$inboundSchema,
};

export const tool$retrospectivesExportIncidentRetrospectives: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-export-incident-retrospectives",
  description: `Export an incident's retrospective(s)

Export incident's retrospective(s) using their templates`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesExportIncidentRetrospectives(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
