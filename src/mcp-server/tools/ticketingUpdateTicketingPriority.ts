/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingUpdateTicketingPriority } from "../../funcs/ticketingUpdateTicketingPriority.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTicketingPriorityRequest$inboundSchema,
};

export const tool$ticketingUpdateTicketingPriority: ToolDefinition<
  typeof args
> = {
  name: "ticketing-update-ticketing-priority",
  description: `Update a ticketing priority

Update a single ticketing priority's attributes`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingUpdateTicketingPriority(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
