/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesDeleteChange } from "../../funcs/changesDeleteChange.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteChangeRequest$inboundSchema,
};

export const tool$changesDeleteChange: ToolDefinition<typeof args> = {
  name: "changes-delete-change",
  description: `Archive a change entry

Archive a change entry`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesDeleteChange(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
