/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsUnarchiveIncident } from "../../funcs/incidentsUnarchiveIncident.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UnarchiveIncidentRequest$inboundSchema,
};

export const tool$incidentsUnarchiveIncident: ToolDefinition<typeof args> = {
  name: "incidents-unarchive-incident",
  description: `Unarchive an incident

Unarchive an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsUnarchiveIncident(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
