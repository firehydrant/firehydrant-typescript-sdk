/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsGetStatuspageConnection } from "../../funcs/integrationsGetStatuspageConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStatuspageConnectionRequest$inboundSchema,
};

export const tool$integrationsGetStatuspageConnection: ToolDefinition<
  typeof args
> = {
  name: "integrations-get-statuspage-connection",
  description: `Get a Statuspage connection

Retrieve the information about the Statuspage connection.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsGetStatuspageConnection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
