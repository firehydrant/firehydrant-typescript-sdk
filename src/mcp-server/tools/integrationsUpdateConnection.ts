/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsUpdateConnection } from "../../funcs/integrationsUpdateConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateConnectionRequest$inboundSchema,
};

export const tool$integrationsUpdateConnection: ToolDefinition<typeof args> = {
  name: "integrations-update-connection",
  description: `Update an integration connection

Update the integration connection with the provided data`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsUpdateConnection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
