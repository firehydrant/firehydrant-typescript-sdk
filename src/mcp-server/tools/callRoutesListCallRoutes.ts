/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { callRoutesListCallRoutes } from "../../funcs/callRoutesListCallRoutes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCallRoutesRequest$inboundSchema,
};

export const tool$callRoutesListCallRoutes: ToolDefinition<typeof args> = {
  name: "call-routes-list-call-routes",
  description: `List call routes

List call routes for the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await callRoutesListCallRoutes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
