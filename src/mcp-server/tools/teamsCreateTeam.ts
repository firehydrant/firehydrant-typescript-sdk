/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsCreateTeam } from "../../funcs/teamsCreateTeam.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateTeam$inboundSchema,
};

export const tool$teamsCreateTeam: ToolDefinition<typeof args> = {
  name: "teams-create-team",
  description: `Create a team

Create a new team`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamsCreateTeam(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
