/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsDeleteSignalsHeartbeatEndpointConfiguration } from "../../funcs/signalsDeleteSignalsHeartbeatEndpointConfiguration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.DeleteSignalsHeartbeatEndpointConfigurationRequest$inboundSchema,
};

export const tool$signalsDeleteSignalsHeartbeatEndpointConfiguration:
  ToolDefinition<typeof args> = {
    name: "signals-delete-signals-heartbeat-endpoint-configuration",
    description: `Delete a heartbeat endpoint configuration

Delete a heartbeat endpoint configuration`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await signalsDeleteSignalsHeartbeatEndpointConfiguration(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
