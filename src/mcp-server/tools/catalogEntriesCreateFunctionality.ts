/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesCreateFunctionality } from "../../funcs/catalogEntriesCreateFunctionality.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateFunctionality$inboundSchema,
};

export const tool$catalogEntriesCreateFunctionality: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-create-functionality",
  description: `Create a functionality

Creates a functionality for the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesCreateFunctionality(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
