/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesDeleteIncidentStatusPage } from "../../funcs/statusPagesDeleteIncidentStatusPage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteIncidentStatusPageRequest$inboundSchema,
};

export const tool$statusPagesDeleteIncidentStatusPage: ToolDefinition<
  typeof args
> = {
  name: "status-pages-delete-incident-status-page",
  description: `Remove a status page from an incident

Remove a status page incident attached to an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesDeleteIncidentStatusPage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
