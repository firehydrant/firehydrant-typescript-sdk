/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsUpdateTeamEscalationPolicy } from "../../funcs/signalsUpdateTeamEscalationPolicy.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTeamEscalationPolicyRequest$inboundSchema,
};

export const tool$signalsUpdateTeamEscalationPolicy: ToolDefinition<
  typeof args
> = {
  name: "signals-update-team-escalation-policy",
  description: `Update an escalation policy for a team

Update a Signals escalation policy by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsUpdateTeamEscalationPolicy(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
