/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsGetSeverity } from "../../funcs/incidentSettingsGetSeverity.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSeverityRequest$inboundSchema,
};

export const tool$incidentSettingsGetSeverity: ToolDefinition<typeof args> = {
  name: "incident-settings-get-severity",
  description: `Get a severity

Retrieve a specific severity`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsGetSeverity(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
