/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsListSlackEmojiActions } from "../../funcs/integrationsListSlackEmojiActions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListSlackEmojiActionsRequest$inboundSchema,
};

export const tool$integrationsListSlackEmojiActions: ToolDefinition<
  typeof args
> = {
  name: "integrations-list-slack-emoji-actions",
  description: `List Slack emoji actions

Lists Slack emoji actions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsListSlackEmojiActions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
