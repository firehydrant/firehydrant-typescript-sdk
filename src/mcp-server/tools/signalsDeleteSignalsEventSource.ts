/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsDeleteSignalsEventSource } from "../../funcs/signalsDeleteSignalsEventSource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSignalsEventSourceRequest$inboundSchema,
};

export const tool$signalsDeleteSignalsEventSource: ToolDefinition<typeof args> =
  {
    name: "signals-delete-signals-event-source",
    description: `Delete an event source for Signals

Delete a Signals event source by slug`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsDeleteSignalsEventSource(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
