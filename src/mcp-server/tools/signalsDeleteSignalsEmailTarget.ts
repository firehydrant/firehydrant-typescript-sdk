/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsDeleteSignalsEmailTarget } from "../../funcs/signalsDeleteSignalsEmailTarget.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSignalsEmailTargetRequest$inboundSchema,
};

export const tool$signalsDeleteSignalsEmailTarget: ToolDefinition<typeof args> =
  {
    name: "signals-delete-signals-email-target",
    description: `Delete a signal email target

Delete a Signals email target by ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsDeleteSignalsEmailTarget(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
