/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksListChecklistTemplates } from "../../funcs/tasksListChecklistTemplates.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListChecklistTemplatesRequest$inboundSchema,
};

export const tool$tasksListChecklistTemplates: ToolDefinition<typeof args> = {
  name: "tasks-list-checklist-templates",
  description: `List checklist templates

List all of the checklist templates that have been added to the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksListChecklistTemplates(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
