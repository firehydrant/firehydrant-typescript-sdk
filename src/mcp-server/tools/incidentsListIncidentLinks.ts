/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsListIncidentLinks } from "../../funcs/incidentsListIncidentLinks.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentLinksRequest$inboundSchema,
};

export const tool$incidentsListIncidentLinks: ToolDefinition<typeof args> = {
  name: "incidents-list-incident-links",
  description: `List links on an incident

List all the editable, external incident links attached to an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsListIncidentLinks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
