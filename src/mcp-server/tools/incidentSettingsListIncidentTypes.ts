/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsListIncidentTypes } from "../../funcs/incidentSettingsListIncidentTypes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIncidentTypesRequest$inboundSchema,
};

export const tool$incidentSettingsListIncidentTypes: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-list-incident-types",
  description: `List incident types

List all of the incident types in the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsListIncidentTypes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
