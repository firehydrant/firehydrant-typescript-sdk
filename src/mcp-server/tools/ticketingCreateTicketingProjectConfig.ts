/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingCreateTicketingProjectConfig } from "../../funcs/ticketingCreateTicketingProjectConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTicketingProjectConfigRequest$inboundSchema,
};

export const tool$ticketingCreateTicketingProjectConfig: ToolDefinition<
  typeof args
> = {
  name: "ticketing-create-ticketing-project-config",
  description: `Create a ticketing project configuration

Creates configuration for a ticketing project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingCreateTicketingProjectConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
