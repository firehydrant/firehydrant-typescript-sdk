/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationsDeleteCommentReaction } from "../../funcs/conversationsDeleteCommentReaction.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteCommentReactionRequest$inboundSchema,
};

export const tool$conversationsDeleteCommentReaction: ToolDefinition<
  typeof args
> = {
  name: "conversations-delete-comment-reaction",
  description: `Delete a reaction from a conversation comment

ALPHA - Archive a reaction`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await conversationsDeleteCommentReaction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
