/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksListWebhookDeliveries } from "../../funcs/webhooksListWebhookDeliveries.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListWebhookDeliveriesRequest$inboundSchema,
};

export const tool$webhooksListWebhookDeliveries: ToolDefinition<typeof args> = {
  name: "webhooks-list-webhook-deliveries",
  description: `List webhook deliveries

Get webhook deliveries`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksListWebhookDeliveries(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
