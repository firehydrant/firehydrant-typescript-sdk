/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountSettingsGetAiPreferences } from "../../funcs/accountSettingsGetAiPreferences.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$accountSettingsGetAiPreferences: ToolDefinition = {
  name: "account-settings-get-ai-preferences",
  description: `Get AI preferences

Retrieves the current AI preferences`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await accountSettingsGetAiPreferences(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
