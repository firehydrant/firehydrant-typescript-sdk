/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetSignalsEmailTarget } from "../../funcs/signalsGetSignalsEmailTarget.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSignalsEmailTargetRequest$inboundSchema,
};

export const tool$signalsGetSignalsEmailTarget: ToolDefinition<typeof args> = {
  name: "signals-get-signals-email-target",
  description: `Get a signal email target

Get a Signals email target by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsGetSignalsEmailTarget(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
