/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsCloseIncident } from "../../funcs/incidentsCloseIncident.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CloseIncidentRequest$inboundSchema,
};

export const tool$incidentsCloseIncident: ToolDefinition<typeof args> = {
  name: "incidents-close-incident",
  description: `Close an incident

Closes an incident and optionally close all children`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsCloseIncident(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
