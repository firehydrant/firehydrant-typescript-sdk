/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { callRoutesDeleteCallRoute } from "../../funcs/callRoutesDeleteCallRoute.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteCallRouteRequest$inboundSchema,
};

export const tool$callRoutesDeleteCallRoute: ToolDefinition<typeof args> = {
  name: "call-routes-delete-call-route",
  description: `Delete a call route

Delete a call route by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await callRoutesDeleteCallRoute(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
