/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesDeleteIncidentRetrospectiveDynamicInput } from "../../funcs/retrospectivesDeleteIncidentRetrospectiveDynamicInput.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.DeleteIncidentRetrospectiveDynamicInputRequest$inboundSchema,
};

export const tool$retrospectivesDeleteIncidentRetrospectiveDynamicInput:
  ToolDefinition<typeof args> = {
    name: "retrospectives-delete-incident-retrospective-dynamic-input",
    description:
      `Removes a dynamic input from a retrospective's dynamic input group field

Delete a dynamic input on a dynamic input group`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await retrospectivesDeleteIncidentRetrospectiveDynamicInput(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
