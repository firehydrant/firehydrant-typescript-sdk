/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingExportSignalsShiftAnalytics } from "../../funcs/metricsReportingExportSignalsShiftAnalytics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ExportSignalsShiftAnalyticsRequest$inboundSchema,
};

export const tool$metricsReportingExportSignalsShiftAnalytics: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-export-signals-shift-analytics",
  description: `Export on-call hours report

Export on-call hours report for users/teams during a time period`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsReportingExportSignalsShiftAnalytics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
