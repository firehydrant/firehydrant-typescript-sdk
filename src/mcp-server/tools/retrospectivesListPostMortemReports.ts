/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesListPostMortemReports } from "../../funcs/retrospectivesListPostMortemReports.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPostMortemReportsRequest$inboundSchema,
};

export const tool$retrospectivesListPostMortemReports: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-list-post-mortem-reports",
  description: `List retrospective reports

List all reports`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesListPostMortemReports(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
