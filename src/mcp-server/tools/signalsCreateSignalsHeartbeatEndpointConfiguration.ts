/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsCreateSignalsHeartbeatEndpointConfiguration } from "../../funcs/signalsCreateSignalsHeartbeatEndpointConfiguration.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateSignalsHeartbeatEndpointConfiguration$inboundSchema,
};

export const tool$signalsCreateSignalsHeartbeatEndpointConfiguration:
  ToolDefinition<typeof args> = {
    name: "signals-create-signals-heartbeat-endpoint-configuration",
    description: `Create a heartbeat endpoint configuration

Create a new heartbeat endpoint configuration for your organization`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await signalsCreateSignalsHeartbeatEndpointConfiguration(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
