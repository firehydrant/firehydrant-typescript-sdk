/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksGetRunbook } from "../../funcs/runbooksGetRunbook.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRunbookRequest$inboundSchema,
};

export const tool$runbooksGetRunbook: ToolDefinition<typeof args> = {
  name: "runbooks-get-runbook",
  description: `Get a runbook

Get a runbook and all its configuration`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runbooksGetRunbook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
