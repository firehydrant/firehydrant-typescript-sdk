/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingListTicketingProjects } from "../../funcs/ticketingListTicketingProjects.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTicketingProjectsRequest$inboundSchema,
};

export const tool$ticketingListTicketingProjects: ToolDefinition<typeof args> =
  {
    name: "ticketing-list-ticketing-projects",
    description: `List ticketing projects

List all ticketing projects available to the organization`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await ticketingListTicketingProjects(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
