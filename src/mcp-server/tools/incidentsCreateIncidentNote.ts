/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsCreateIncidentNote } from "../../funcs/incidentsCreateIncidentNote.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateIncidentNoteRequest$inboundSchema,
};

export const tool$incidentsCreateIncidentNote: ToolDefinition<typeof args> = {
  name: "incidents-create-incident-note",
  description: `Add a note to an incident

Create a new note on for an incident. The visibility field on a note determines where it gets posted.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsCreateIncidentNote(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
