/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsCreateConnection } from "../../funcs/integrationsCreateConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateConnectionRequest$inboundSchema,
};

export const tool$integrationsCreateConnection: ToolDefinition<typeof args> = {
  name: "integrations-create-connection",
  description: `Create a new integration connection

Create a new integration connection`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsCreateConnection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
