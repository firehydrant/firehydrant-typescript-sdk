/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsListAuthedProviders } from "../../funcs/integrationsListAuthedProviders.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAuthedProvidersRequest$inboundSchema,
};

export const tool$integrationsListAuthedProviders: ToolDefinition<typeof args> =
  {
    name: "integrations-list-authed-providers",
    description: `Lists the available and configured integrations

Lists the available and configured integrations`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await integrationsListAuthedProviders(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
