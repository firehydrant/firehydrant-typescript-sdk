/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { permissionsListPermissions } from "../../funcs/permissionsListPermissions.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$permissionsListPermissions: ToolDefinition = {
  name: "permissions-list-permissions",
  description: `List permissions

List all permissions in the organization`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await permissionsListPermissions(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
