/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsCreateIncidentLink } from "../../funcs/incidentsCreateIncidentLink.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateIncidentLinkRequest$inboundSchema,
};

export const tool$incidentsCreateIncidentLink: ToolDefinition<typeof args> = {
  name: "incidents-create-incident-link",
  description: `Add a link to an incident

Allows adding adhoc links to an incident as an attachment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsCreateIncidentLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
