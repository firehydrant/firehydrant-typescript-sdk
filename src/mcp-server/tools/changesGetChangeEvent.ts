/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesGetChangeEvent } from "../../funcs/changesGetChangeEvent.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetChangeEventRequest$inboundSchema,
};

export const tool$changesGetChangeEvent: ToolDefinition<typeof args> = {
  name: "changes-get-change-event",
  description: `Get a change event

Retrieve a change event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesGetChangeEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
