/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetSignalsHackerMode } from "../../funcs/signalsGetSignalsHackerMode.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$signalsGetSignalsHackerMode: ToolDefinition = {
  name: "signals-get-signals-hacker-mode",
  description: `Get hacker mode status

Get the status of the hacker mode for the current user`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await signalsGetSignalsHackerMode(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
