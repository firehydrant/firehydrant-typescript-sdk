/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsUpdateIncident } from "../../funcs/incidentsUpdateIncident.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateIncidentRequest$inboundSchema,
};

export const tool$incidentsUpdateIncident: ToolDefinition<typeof args> = {
  name: "incidents-update-incident",
  description: `Update an incident

Updates an incident with provided parameters`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsUpdateIncident(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
