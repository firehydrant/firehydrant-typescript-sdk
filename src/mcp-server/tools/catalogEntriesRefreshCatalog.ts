/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesRefreshCatalog } from "../../funcs/catalogEntriesRefreshCatalog.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RefreshCatalogRequest$inboundSchema,
};

export const tool$catalogEntriesRefreshCatalog: ToolDefinition<typeof args> = {
  name: "catalog-entries-refresh-catalog",
  description: `Refresh a service catalog

Schedules an async task to re-import catalog info and update catalog data accordingly.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesRefreshCatalog(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
