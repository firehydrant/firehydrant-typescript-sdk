/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { callRoutesListTeamCallRoutes } from "../../funcs/callRoutesListTeamCallRoutes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTeamCallRoutesRequest$inboundSchema,
};

export const tool$callRoutesListTeamCallRoutes: ToolDefinition<typeof args> = {
  name: "call-routes-list-team-call-routes",
  description: `List call routes for a team

List call routes for a team`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await callRoutesListTeamCallRoutes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
