/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsDeleteScheduledMaintenance } from "../../funcs/incidentsDeleteScheduledMaintenance.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteScheduledMaintenanceRequest$inboundSchema,
};

export const tool$incidentsDeleteScheduledMaintenance: ToolDefinition<
  typeof args
> = {
  name: "incidents-delete-scheduled-maintenance",
  description: `Delete a scheduled maintenance event

Delete a scheduled maintenance event, preventing it from taking place.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsDeleteScheduledMaintenance(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
