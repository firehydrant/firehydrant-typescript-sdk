/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsUpdateCustomFieldDefinition } from "../../funcs/incidentSettingsUpdateCustomFieldDefinition.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCustomFieldDefinitionRequest$inboundSchema,
};

export const tool$incidentSettingsUpdateCustomFieldDefinition: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-update-custom-field-definition",
  description: `Update a custom field definition

Update a single custom field definition`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsUpdateCustomFieldDefinition(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
