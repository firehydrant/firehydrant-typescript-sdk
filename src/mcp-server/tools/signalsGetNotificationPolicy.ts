/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetNotificationPolicy } from "../../funcs/signalsGetNotificationPolicy.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetNotificationPolicyRequest$inboundSchema,
};

export const tool$signalsGetNotificationPolicy: ToolDefinition<typeof args> = {
  name: "signals-get-notification-policy",
  description: `Get a notification policy

Get a Signals notification policy by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsGetNotificationPolicy(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
