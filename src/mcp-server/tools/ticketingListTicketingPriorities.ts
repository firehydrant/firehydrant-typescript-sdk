/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingListTicketingPriorities } from "../../funcs/ticketingListTicketingPriorities.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$ticketingListTicketingPriorities: ToolDefinition = {
  name: "ticketing-list-ticketing-priorities",
  description: `List ticketing priorities

List all ticketing priorities available to the organization`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await ticketingListTicketingPriorities(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
