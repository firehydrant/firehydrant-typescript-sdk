/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesUpdateRetrospectiveTemplate } from "../../funcs/retrospectivesUpdateRetrospectiveTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateRetrospectiveTemplateRequest$inboundSchema,
};

export const tool$retrospectivesUpdateRetrospectiveTemplate: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-update-retrospective-template",
  description: `Update a retrospective template

Update a single retrospective template`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesUpdateRetrospectiveTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
