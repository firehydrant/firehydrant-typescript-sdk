/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsListCustomFieldDefinitions } from "../../funcs/incidentSettingsListCustomFieldDefinitions.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$incidentSettingsListCustomFieldDefinitions: ToolDefinition = {
  name: "incident-settings-list-custom-field-definitions",
  description: `List custom field definitions

List all custom field definitions`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await incidentSettingsListCustomFieldDefinitions(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
