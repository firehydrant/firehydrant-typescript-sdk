/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingListAvailableInboundFieldMaps } from "../../funcs/ticketingListAvailableInboundFieldMaps.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAvailableInboundFieldMapsRequest$inboundSchema,
};

export const tool$ticketingListAvailableInboundFieldMaps: ToolDefinition<
  typeof args
> = {
  name: "ticketing-list-available-inbound-field-maps",
  description: `List available fields for ticket field mapping

Returns metadata for the fields that are available for inbound field mapping.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingListAvailableInboundFieldMaps(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
