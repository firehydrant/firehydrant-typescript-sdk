/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesListRetrospectives } from "../../funcs/retrospectivesListRetrospectives.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRetrospectivesRequest$inboundSchema,
};

export const tool$retrospectivesListRetrospectives: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-list-retrospectives",
  description: `List retrospective reports

List all retrospective reports`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesListRetrospectives(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
