/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsUpdateSeverityMatrixImpact } from "../../funcs/incidentSettingsUpdateSeverityMatrixImpact.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSeverityMatrixImpactRequest$inboundSchema,
};

export const tool$incidentSettingsUpdateSeverityMatrixImpact: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-update-severity-matrix-impact",
  description: `Update a severity matrix impact

Update a severity matrix impact`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsUpdateSeverityMatrixImpact(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
