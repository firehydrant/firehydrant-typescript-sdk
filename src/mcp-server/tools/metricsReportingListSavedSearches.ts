/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingListSavedSearches } from "../../funcs/metricsReportingListSavedSearches.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListSavedSearchesRequest$inboundSchema,
};

export const tool$metricsReportingListSavedSearches: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-list-saved-searches",
  description: `List saved searches

Lists saved searches`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsReportingListSavedSearches(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
