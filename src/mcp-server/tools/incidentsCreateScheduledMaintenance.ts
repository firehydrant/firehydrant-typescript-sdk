/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsCreateScheduledMaintenance } from "../../funcs/incidentsCreateScheduledMaintenance.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateScheduledMaintenance$inboundSchema,
};

export const tool$incidentsCreateScheduledMaintenance: ToolDefinition<
  typeof args
> = {
  name: "incidents-create-scheduled-maintenance",
  description: `Create a scheduled maintenance event

Create a new scheduled maintenance event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsCreateScheduledMaintenance(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
