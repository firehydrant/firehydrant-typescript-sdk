/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesListEmailSubscribers } from "../../funcs/statusPagesListEmailSubscribers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListEmailSubscribersRequest$inboundSchema,
};

export const tool$statusPagesListEmailSubscribers: ToolDefinition<typeof args> =
  {
    name: "status-pages-list-email-subscribers",
    description: `List status page subscribers

Retrieves the list of subscribers for a status page.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await statusPagesListEmailSubscribers(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
