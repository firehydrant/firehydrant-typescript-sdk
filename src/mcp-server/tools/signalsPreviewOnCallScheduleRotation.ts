/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsPreviewOnCallScheduleRotation } from "../../funcs/signalsPreviewOnCallScheduleRotation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PreviewOnCallScheduleRotationRequest$inboundSchema,
};

export const tool$signalsPreviewOnCallScheduleRotation: ToolDefinition<
  typeof args
> = {
  name: "signals-preview-on-call-schedule-rotation",
  description: `Preview an on-call rotation

Preview a new on-call rotation orchanges to an existing on-call rotation`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsPreviewOnCallScheduleRotation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
