/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesDeleteChangeIdentity } from "../../funcs/changesDeleteChangeIdentity.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteChangeIdentityRequest$inboundSchema,
};

export const tool$changesDeleteChangeIdentity: ToolDefinition<typeof args> = {
  name: "changes-delete-change-identity",
  description: `Delete an identity from a change entry

Delete an identity from the change entry`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesDeleteChangeIdentity(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
