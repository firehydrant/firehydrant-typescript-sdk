/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsUpdateOnCallScheduleRotation } from "../../funcs/signalsUpdateOnCallScheduleRotation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateOnCallScheduleRotationRequest$inboundSchema,
};

export const tool$signalsUpdateOnCallScheduleRotation: ToolDefinition<
  typeof args
> = {
  name: "signals-update-on-call-schedule-rotation",
  description: `Update an on-call schedule's rotation

Update an on-call schedule's rotation by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsUpdateOnCallScheduleRotation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
