/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingDeleteTicketingProjectConfig } from "../../funcs/ticketingDeleteTicketingProjectConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTicketingProjectConfigRequest$inboundSchema,
};

export const tool$ticketingDeleteTicketingProjectConfig: ToolDefinition<
  typeof args
> = {
  name: "ticketing-delete-ticketing-project-config",
  description: `Archive a ticketing project configuration

Archive configuration for a ticketing project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingDeleteTicketingProjectConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
