/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsCreateSignalsEventSource } from "../../funcs/signalsCreateSignalsEventSource.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateSignalsEventSource$inboundSchema,
};

export const tool$signalsCreateSignalsEventSource: ToolDefinition<typeof args> =
  {
    name: "signals-create-signals-event-source",
    description: `Create an event source for Signals

Create a Signals event source for the authenticated user.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsCreateSignalsEventSource(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
