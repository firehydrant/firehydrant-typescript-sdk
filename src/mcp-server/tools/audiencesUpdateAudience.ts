/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { audiencesUpdateAudience } from "../../funcs/audiencesUpdateAudience.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAudienceRequest$inboundSchema,
};

export const tool$audiencesUpdateAudience: ToolDefinition<typeof args> = {
  name: "audiences-update-audience",
  description: `Update audience

Update an existing audience`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await audiencesUpdateAudience(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
