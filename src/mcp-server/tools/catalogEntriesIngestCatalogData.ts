/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesIngestCatalogData } from "../../funcs/catalogEntriesIngestCatalogData.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.IngestCatalogDataRequest$inboundSchema,
};

export const tool$catalogEntriesIngestCatalogData: ToolDefinition<typeof args> =
  {
    name: "catalog-entries-ingest-catalog-data",
    description: `Ingest service catalog data

Accepts catalog data in the configured format and asyncronously processes the data to incorporate changes into service catalog.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await catalogEntriesIngestCatalogData(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
