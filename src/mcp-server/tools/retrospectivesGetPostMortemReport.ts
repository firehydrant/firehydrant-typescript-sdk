/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesGetPostMortemReport } from "../../funcs/retrospectivesGetPostMortemReport.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPostMortemReportRequest$inboundSchema,
};

export const tool$retrospectivesGetPostMortemReport: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-get-post-mortem-report",
  description: `Get a retrospective report

Get a report`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesGetPostMortemReport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
