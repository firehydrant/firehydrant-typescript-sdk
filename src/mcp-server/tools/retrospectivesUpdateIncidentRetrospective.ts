/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesUpdateIncidentRetrospective } from "../../funcs/retrospectivesUpdateIncidentRetrospective.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateIncidentRetrospectiveRequest$inboundSchema,
};

export const tool$retrospectivesUpdateIncidentRetrospective: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-update-incident-retrospective",
  description: `Update a retrospective on the incident

Update a retrospective attached to an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesUpdateIncidentRetrospective(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
