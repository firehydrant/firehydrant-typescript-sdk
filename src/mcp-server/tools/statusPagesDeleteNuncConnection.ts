/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesDeleteNuncConnection } from "../../funcs/statusPagesDeleteNuncConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteNuncConnectionRequest$inboundSchema,
};

export const tool$statusPagesDeleteNuncConnection: ToolDefinition<typeof args> =
  {
    name: "status-pages-delete-nunc-connection",
    description: `Delete a status page

Delete a FireHydrant hosted status page, stopping updates of your incidents to it.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await statusPagesDeleteNuncConnection(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
