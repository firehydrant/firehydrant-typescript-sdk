/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsListTeamSignalRules } from "../../funcs/signalsListTeamSignalRules.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTeamSignalRulesRequest$inboundSchema,
};

export const tool$signalsListTeamSignalRules: ToolDefinition<typeof args> = {
  name: "signals-list-team-signal-rules",
  description: `List Signals rules

List all Signals rules for a team.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsListTeamSignalRules(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
