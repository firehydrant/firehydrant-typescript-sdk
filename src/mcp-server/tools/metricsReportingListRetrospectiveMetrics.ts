/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingListRetrospectiveMetrics } from "../../funcs/metricsReportingListRetrospectiveMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRetrospectiveMetricsRequest$inboundSchema,
};

export const tool$metricsReportingListRetrospectiveMetrics: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-list-retrospective-metrics",
  description: `List retrospective metrics

Returns a report with retrospective analytics data`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsReportingListRetrospectiveMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
