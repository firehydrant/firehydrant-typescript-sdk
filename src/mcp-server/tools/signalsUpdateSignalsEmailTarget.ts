/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsUpdateSignalsEmailTarget } from "../../funcs/signalsUpdateSignalsEmailTarget.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSignalsEmailTargetRequest$inboundSchema,
};

export const tool$signalsUpdateSignalsEmailTarget: ToolDefinition<typeof args> =
  {
    name: "signals-update-signals-email-target",
    description: `Update an email target

Update a Signals email target by ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsUpdateSignalsEmailTarget(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
