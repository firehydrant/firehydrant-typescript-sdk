/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingUpdateTicketingFieldMap } from "../../funcs/ticketingUpdateTicketingFieldMap.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTicketingFieldMapRequest$inboundSchema,
};

export const tool$ticketingUpdateTicketingFieldMap: ToolDefinition<
  typeof args
> = {
  name: "ticketing-update-ticketing-field-map",
  description: `Update a field map for a ticketing project

Update field map for a ticketing project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingUpdateTicketingFieldMap(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
