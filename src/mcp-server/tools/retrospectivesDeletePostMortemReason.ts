/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesDeletePostMortemReason } from "../../funcs/retrospectivesDeletePostMortemReason.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeletePostMortemReasonRequest$inboundSchema,
};

export const tool$retrospectivesDeletePostMortemReason: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-delete-post-mortem-reason",
  description: `Delete a contributing factor from a retrospective report

Delete a contributing factor`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesDeletePostMortemReason(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
