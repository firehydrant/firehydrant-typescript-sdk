/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsListAwsCloudtrailBatchEvents } from "../../funcs/integrationsListAwsCloudtrailBatchEvents.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAwsCloudtrailBatchEventsRequest$inboundSchema,
};

export const tool$integrationsListAwsCloudtrailBatchEvents: ToolDefinition<
  typeof args
> = {
  name: "integrations-list-aws-cloudtrail-batch-events",
  description: `List events for an AWS CloudTrail batch

List events for an AWS CloudTrail batch`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsListAwsCloudtrailBatchEvents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
