/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksUpdateChecklistTemplate } from "../../funcs/tasksUpdateChecklistTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateChecklistTemplateRequest$inboundSchema,
};

export const tool$tasksUpdateChecklistTemplate: ToolDefinition<typeof args> = {
  name: "tasks-update-checklist-template",
  description: `Update a checklist template

Update a checklist templates attributes`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksUpdateChecklistTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
