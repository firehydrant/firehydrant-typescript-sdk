/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingUpdateSavedSearch } from "../../funcs/metricsReportingUpdateSavedSearch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSavedSearchRequest$inboundSchema,
};

export const tool$metricsReportingUpdateSavedSearch: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-update-saved-search",
  description: `Update a saved search

Update a specific saved search`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsReportingUpdateSavedSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
