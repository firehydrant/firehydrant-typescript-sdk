/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesUpdateChangeEvent } from "../../funcs/changesUpdateChangeEvent.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateChangeEventRequest$inboundSchema,
};

export const tool$changesUpdateChangeEvent: ToolDefinition<typeof args> = {
  name: "changes-update-change-event",
  description: `Update a change event

Update a change event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesUpdateChangeEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
