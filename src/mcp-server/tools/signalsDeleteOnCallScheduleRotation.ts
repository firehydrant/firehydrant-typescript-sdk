/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsDeleteOnCallScheduleRotation } from "../../funcs/signalsDeleteOnCallScheduleRotation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteOnCallScheduleRotationRequest$inboundSchema,
};

export const tool$signalsDeleteOnCallScheduleRotation: ToolDefinition<
  typeof args
> = {
  name: "signals-delete-on-call-schedule-rotation",
  description: `Delete an on-call schedule's rotation

Delete an on-call schedule's rotation by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsDeleteOnCallScheduleRotation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
