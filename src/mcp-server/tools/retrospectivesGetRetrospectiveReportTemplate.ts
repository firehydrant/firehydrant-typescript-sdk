/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesGetRetrospectiveReportTemplate } from "../../funcs/retrospectivesGetRetrospectiveReportTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRetrospectiveReportTemplateRequest$inboundSchema,
};

export const tool$retrospectivesGetRetrospectiveReportTemplate: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-get-retrospective-report-template",
  description: `Get a retrospective report template

Retrieve a single retrospective report template by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await retrospectivesGetRetrospectiveReportTemplate(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
