/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsGetConferenceBridgeTranslation } from "../../funcs/incidentsGetConferenceBridgeTranslation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetConferenceBridgeTranslationRequest$inboundSchema,
};

export const tool$incidentsGetConferenceBridgeTranslation: ToolDefinition<
  typeof args
> = {
  name: "incidents-get-conference-bridge-translation",
  description: `Retrieve the translations for a specific conference bridge

Retrieve the translations for a specific conference bridge`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsGetConferenceBridgeTranslation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
