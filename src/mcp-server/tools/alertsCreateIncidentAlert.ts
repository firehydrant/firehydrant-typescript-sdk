/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { alertsCreateIncidentAlert } from "../../funcs/alertsCreateIncidentAlert.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateIncidentAlertRequest$inboundSchema,
};

export const tool$alertsCreateIncidentAlert: ToolDefinition<typeof args> = {
  name: "alerts-create-incident-alert",
  description: `Attach an alert to an incident

Add an alert to an incident. FireHydrant needs to have ingested the alert from a third party system in order to attach it to the incident.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await alertsCreateIncidentAlert(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
