/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsListNotificationPolicySettings } from "../../funcs/signalsListNotificationPolicySettings.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListNotificationPolicySettingsRequest$inboundSchema,
};

export const tool$signalsListNotificationPolicySettings: ToolDefinition<
  typeof args
> = {
  name: "signals-list-notification-policy-settings",
  description: `List notification policies

List all Signals notification policies.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsListNotificationPolicySettings(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
