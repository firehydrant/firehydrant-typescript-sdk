/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesDeleteEmailSubscriber } from "../../funcs/statusPagesDeleteEmailSubscriber.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteEmailSubscriberRequest$inboundSchema,
};

export const tool$statusPagesDeleteEmailSubscriber: ToolDefinition<
  typeof args
> = {
  name: "status-pages-delete-email-subscriber",
  description: `Remove subscribers from a status page

Unsubscribes one or more status page subscribers.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesDeleteEmailSubscriber(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
