/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetOnCallScheduleRotation } from "../../funcs/signalsGetOnCallScheduleRotation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOnCallScheduleRotationRequest$inboundSchema,
};

export const tool$signalsGetOnCallScheduleRotation: ToolDefinition<
  typeof args
> = {
  name: "signals-get-on-call-schedule-rotation",
  description: `Get an on-call rotation

Get an on-call rotation by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsGetOnCallScheduleRotation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
