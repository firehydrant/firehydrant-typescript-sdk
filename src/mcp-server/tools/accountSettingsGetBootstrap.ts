/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountSettingsGetBootstrap } from "../../funcs/accountSettingsGetBootstrap.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$accountSettingsGetBootstrap: ToolDefinition = {
  name: "account-settings-get-bootstrap",
  description: `Get initial application configuration

Get initial application configuration`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await accountSettingsGetBootstrap(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
