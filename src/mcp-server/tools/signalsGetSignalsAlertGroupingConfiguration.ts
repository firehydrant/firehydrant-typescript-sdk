/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetSignalsAlertGroupingConfiguration } from "../../funcs/signalsGetSignalsAlertGroupingConfiguration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSignalsAlertGroupingConfigurationRequest$inboundSchema,
};

export const tool$signalsGetSignalsAlertGroupingConfiguration: ToolDefinition<
  typeof args
> = {
  name: "signals-get-signals-alert-grouping-configuration",
  description: `Get an alert grouping configuration.

Get a Signals alert grouping rule by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsGetSignalsAlertGroupingConfiguration(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
