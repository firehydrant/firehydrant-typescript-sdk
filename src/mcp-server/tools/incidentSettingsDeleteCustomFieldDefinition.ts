/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsDeleteCustomFieldDefinition } from "../../funcs/incidentSettingsDeleteCustomFieldDefinition.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteCustomFieldDefinitionRequest$inboundSchema,
};

export const tool$incidentSettingsDeleteCustomFieldDefinition: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-delete-custom-field-definition",
  description: `Delete a custom field definition

Delete a custom field definition`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsDeleteCustomFieldDefinition(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
