/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { audiencesGenerateAudienceSummary } from "../../funcs/audiencesGenerateAudienceSummary.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GenerateAudienceSummaryRequest$inboundSchema,
};

export const tool$audiencesGenerateAudienceSummary: ToolDefinition<
  typeof args
> = {
  name: "audiences-generate-audience-summary",
  description: `Generate summary

Generate a new audience-specific summary for an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await audiencesGenerateAudienceSummary(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
