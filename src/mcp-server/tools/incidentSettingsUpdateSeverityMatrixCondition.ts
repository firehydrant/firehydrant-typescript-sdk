/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsUpdateSeverityMatrixCondition } from "../../funcs/incidentSettingsUpdateSeverityMatrixCondition.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSeverityMatrixConditionRequest$inboundSchema,
};

export const tool$incidentSettingsUpdateSeverityMatrixCondition: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-update-severity-matrix-condition",
  description: `Update a severity matrix condition

Update a severity matrix condition`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await incidentSettingsUpdateSeverityMatrixCondition(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
