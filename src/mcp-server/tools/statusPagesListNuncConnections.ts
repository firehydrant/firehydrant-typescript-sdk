/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesListNuncConnections } from "../../funcs/statusPagesListNuncConnections.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$statusPagesListNuncConnections: ToolDefinition = {
  name: "status-pages-list-nunc-connections",
  description: `List status pages

Lists the information displayed as part of your FireHydrant hosted status pages.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await statusPagesListNuncConnections(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
