/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scimListSCIMUsers } from "../../funcs/scimListSCIMUsers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListScimUsersRequest$inboundSchema,
};

export const tool$scimListSCIMUsers: ToolDefinition<typeof args> = {
  name: "scim-list-scim-users",
  description: `List SCIM users

SCIM endpoint that lists users. This endpoint will display a list of Users currently in the system.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scimListSCIMUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
