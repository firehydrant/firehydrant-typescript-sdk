/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingUpdateTicketingProjectConfig } from "../../funcs/ticketingUpdateTicketingProjectConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTicketingProjectConfigRequest$inboundSchema,
};

export const tool$ticketingUpdateTicketingProjectConfig: ToolDefinition<
  typeof args
> = {
  name: "ticketing-update-ticketing-project-config",
  description: `Update configuration for a ticketing project

Update configuration for a ticketing project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingUpdateTicketingProjectConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
