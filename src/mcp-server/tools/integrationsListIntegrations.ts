/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsListIntegrations } from "../../funcs/integrationsListIntegrations.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$integrationsListIntegrations: ToolDefinition = {
  name: "integrations-list-integrations",
  description: `List integrations

Lists the available and configured integrations`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await integrationsListIntegrations(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
