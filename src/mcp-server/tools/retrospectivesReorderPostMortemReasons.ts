/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesReorderPostMortemReasons } from "../../funcs/retrospectivesReorderPostMortemReasons.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ReorderPostMortemReasonsRequest$inboundSchema,
};

export const tool$retrospectivesReorderPostMortemReasons: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-reorder-post-mortem-reasons",
  description: `Reorder a contributing factor for a retrospective report

Update the order of contributing factors in a retrospective report`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesReorderPostMortemReasons(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
