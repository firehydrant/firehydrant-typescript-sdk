/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingGetTicketingPriority } from "../../funcs/ticketingGetTicketingPriority.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTicketingPriorityRequest$inboundSchema,
};

export const tool$ticketingGetTicketingPriority: ToolDefinition<typeof args> = {
  name: "ticketing-get-ticketing-priority",
  description: `Get a ticketing priority

Retrieve a single ticketing priority by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingGetTicketingPriority(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
