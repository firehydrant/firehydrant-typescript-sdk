/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountSettingsListEntitlements } from "../../funcs/accountSettingsListEntitlements.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListEntitlementsRequest$inboundSchema,
};

export const tool$accountSettingsListEntitlements: ToolDefinition<typeof args> =
  {
    name: "account-settings-list-entitlements",
    description: `List entitlements

List the organization's entitlements`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await accountSettingsListEntitlements(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
