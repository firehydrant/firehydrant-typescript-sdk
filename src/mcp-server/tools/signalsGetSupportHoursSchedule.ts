/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetSupportHoursSchedule } from "../../funcs/signalsGetSupportHoursSchedule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSupportHoursScheduleRequest$inboundSchema,
};

export const tool$signalsGetSupportHoursSchedule: ToolDefinition<typeof args> =
  {
    name: "signals-get-support-hours-schedule",
    description: `Get support hours schedule

Get support hours schedule for the team`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsGetSupportHoursSchedule(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
