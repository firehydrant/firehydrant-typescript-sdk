/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingDeleteInboundFieldMap } from "../../funcs/ticketingDeleteInboundFieldMap.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteInboundFieldMapRequest$inboundSchema,
};

export const tool$ticketingDeleteInboundFieldMap: ToolDefinition<typeof args> =
  {
    name: "ticketing-delete-inbound-field-map",
    description: `Archive inbound field map for a ticketing project

Archive inbound field map for a ticketing project`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await ticketingDeleteInboundFieldMap(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
