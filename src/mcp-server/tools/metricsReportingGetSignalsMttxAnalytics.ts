/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingGetSignalsMttxAnalytics } from "../../funcs/metricsReportingGetSignalsMttxAnalytics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSignalsMttxAnalyticsRequest$inboundSchema,
};

export const tool$metricsReportingGetSignalsMttxAnalytics: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-get-signals-mttx-analytics",
  description: `Get MTTX analytics for signals

Get mean-time-to-acknowledged (MTTA) and mean-time-to-resolved (MTTR) metrics for Signals alerts`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsReportingGetSignalsMttxAnalytics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
