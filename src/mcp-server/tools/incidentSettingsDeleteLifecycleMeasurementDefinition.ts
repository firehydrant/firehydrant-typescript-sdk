/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsDeleteLifecycleMeasurementDefinition } from "../../funcs/incidentSettingsDeleteLifecycleMeasurementDefinition.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteLifecycleMeasurementDefinitionRequest$inboundSchema,
};

export const tool$incidentSettingsDeleteLifecycleMeasurementDefinition:
  ToolDefinition<typeof args> = {
    name: "incident-settings-delete-lifecycle-measurement-definition",
    description: `Archive a measurement definition

Archives a measurement definition which will hide it from lists and metrics`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await incidentSettingsDeleteLifecycleMeasurementDefinition(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
