/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesDeleteNuncSubscription } from "../../funcs/statusPagesDeleteNuncSubscription.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteNuncSubscriptionRequest$inboundSchema,
};

export const tool$statusPagesDeleteNuncSubscription: ToolDefinition<
  typeof args
> = {
  name: "status-pages-delete-nunc-subscription",
  description: `Unsubscribe from status page notifications

Unsubscribe from status page updates`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesDeleteNuncSubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
