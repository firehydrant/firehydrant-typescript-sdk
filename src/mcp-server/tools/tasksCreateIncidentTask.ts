/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksCreateIncidentTask } from "../../funcs/tasksCreateIncidentTask.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateIncidentTaskRequest$inboundSchema,
};

export const tool$tasksCreateIncidentTask: ToolDefinition<typeof args> = {
  name: "tasks-create-incident-task",
  description: `Create an incident task

Create a task`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksCreateIncidentTask(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
