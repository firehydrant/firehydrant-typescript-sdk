/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsListSeverityMatrixConditions } from "../../funcs/incidentSettingsListSeverityMatrixConditions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListSeverityMatrixConditionsRequest$inboundSchema,
};

export const tool$incidentSettingsListSeverityMatrixConditions: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-list-severity-matrix-conditions",
  description: `List severity matrix conditions

Lists conditions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await incidentSettingsListSeverityMatrixConditions(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
