/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetSignalsIngestUrl } from "../../funcs/signalsGetSignalsIngestUrl.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSignalsIngestUrlRequest$inboundSchema,
};

export const tool$signalsGetSignalsIngestUrl: ToolDefinition<typeof args> = {
  name: "signals-get-signals-ingest-url",
  description: `Get the signals ingestion URL

Retrieve the url for ingesting signals for your organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsGetSignalsIngestUrl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
