/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsUpdateOnCallShift } from "../../funcs/signalsUpdateOnCallShift.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateOnCallShiftRequest$inboundSchema,
};

export const tool$signalsUpdateOnCallShift: ToolDefinition<typeof args> = {
  name: "signals-update-on-call-shift",
  description: `Update an on-call shift for a team schedule

Update a Signals on-call shift by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsUpdateOnCallShift(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
