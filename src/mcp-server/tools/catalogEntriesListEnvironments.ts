/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesListEnvironments } from "../../funcs/catalogEntriesListEnvironments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListEnvironmentsRequest$inboundSchema,
};

export const tool$catalogEntriesListEnvironments: ToolDefinition<typeof args> =
  {
    name: "catalog-entries-list-environments",
    description: `List environments

List all of the environments that have been added to the organiation`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await catalogEntriesListEnvironments(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
