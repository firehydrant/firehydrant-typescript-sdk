/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { callRoutesCreateTeamCallRoute } from "../../funcs/callRoutesCreateTeamCallRoute.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTeamCallRouteRequest$inboundSchema,
};

export const tool$callRoutesCreateTeamCallRoute: ToolDefinition<typeof args> = {
  name: "call-routes-create-team-call-route",
  description: `Create a call route for a team

Create a call route for a team`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await callRoutesCreateTeamCallRoute(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
