/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsCreateIncidentChatMessage } from "../../funcs/incidentsCreateIncidentChatMessage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateIncidentChatMessageRequest$inboundSchema,
};

export const tool$incidentsCreateIncidentChatMessage: ToolDefinition<
  typeof args
> = {
  name: "incidents-create-incident-chat-message",
  description: `Add a chat message to an incident

Create a new generic chat message on an incident timeline. These are independent of any specific chat provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsCreateIncidentChatMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
