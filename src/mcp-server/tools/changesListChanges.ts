/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesListChanges } from "../../funcs/changesListChanges.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListChangesRequest$inboundSchema,
};

export const tool$changesListChanges: ToolDefinition<typeof args> = {
  name: "changes-list-changes",
  description: `List changes

List changes for the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesListChanges(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
