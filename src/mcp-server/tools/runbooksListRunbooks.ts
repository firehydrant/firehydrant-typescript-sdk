/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runbooksListRunbooks } from "../../funcs/runbooksListRunbooks.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRunbooksRequest$inboundSchema,
};

export const tool$runbooksListRunbooks: ToolDefinition<typeof args> = {
  name: "runbooks-list-runbooks",
  description: `List runbooks

Lists all available runbooks.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runbooksListRunbooks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
