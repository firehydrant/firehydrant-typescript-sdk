/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsUpdateIncidentChangeEvent } from "../../funcs/incidentsUpdateIncidentChangeEvent.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateIncidentChangeEventRequest$inboundSchema,
};

export const tool$incidentsUpdateIncidentChangeEvent: ToolDefinition<
  typeof args
> = {
  name: "incidents-update-incident-change-event",
  description: `Update a change attached to an incident

Update a change attached to an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsUpdateIncidentChangeEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
