/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsListSeverityMatrixImpacts } from "../../funcs/incidentSettingsListSeverityMatrixImpacts.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListSeverityMatrixImpactsRequest$inboundSchema,
};

export const tool$incidentSettingsListSeverityMatrixImpacts: ToolDefinition<
  typeof args
> = {
  name: "incident-settings-list-severity-matrix-impacts",
  description: `List severity matrix impacts

Lists impacts`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentSettingsListSeverityMatrixImpacts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
