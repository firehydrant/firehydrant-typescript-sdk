/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsGetSignalsEventSource } from "../../funcs/signalsGetSignalsEventSource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSignalsEventSourceRequest$inboundSchema,
};

export const tool$signalsGetSignalsEventSource: ToolDefinition<typeof args> = {
  name: "signals-get-signals-event-source",
  description: `Get an event source for Signals

Get a Signals event source by slug`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsGetSignalsEventSource(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
