/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsUpdateScheduledMaintenance } from "../../funcs/incidentsUpdateScheduledMaintenance.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateScheduledMaintenanceRequest$inboundSchema,
};

export const tool$incidentsUpdateScheduledMaintenance: ToolDefinition<
  typeof args
> = {
  name: "incidents-update-scheduled-maintenance",
  description: `Update a scheduled maintenance event

Change the conditions of a scheduled maintenance event, including updating any status page announcements of changes.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsUpdateScheduledMaintenance(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
