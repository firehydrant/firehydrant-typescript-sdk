/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { rolesUpdateRole } from "../../funcs/rolesUpdateRole.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateRoleRequest$inboundSchema,
};

export const tool$rolesUpdateRole: ToolDefinition<typeof args> = {
  name: "roles-update-role",
  description: `Update a role

Update a role`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await rolesUpdateRole(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
