/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { changesCreateChangeIdentity } from "../../funcs/changesCreateChangeIdentity.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateChangeIdentityRequest$inboundSchema,
};

export const tool$changesCreateChangeIdentity: ToolDefinition<typeof args> = {
  name: "changes-create-change-identity",
  description: `Create an identity for a change entry

Create an identity for the change entry`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await changesCreateChangeIdentity(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
