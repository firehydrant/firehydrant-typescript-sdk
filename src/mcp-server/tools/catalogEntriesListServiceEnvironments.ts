/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesListServiceEnvironments } from "../../funcs/catalogEntriesListServiceEnvironments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListServiceEnvironmentsRequest$inboundSchema,
};

export const tool$catalogEntriesListServiceEnvironments: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-list-service-environments",
  description: `List environments for a service

List environments for a service`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesListServiceEnvironments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
