/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsCreateIncidentRoleAssignment } from "../../funcs/incidentsCreateIncidentRoleAssignment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateIncidentRoleAssignmentRequest$inboundSchema,
};

export const tool$incidentsCreateIncidentRoleAssignment: ToolDefinition<
  typeof args
> = {
  name: "incidents-create-incident-role-assignment",
  description: `Assign a user to an incident

Assign a role to a user for this incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsCreateIncidentRoleAssignment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
