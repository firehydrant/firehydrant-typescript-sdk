/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesGetFunctionality } from "../../funcs/catalogEntriesGetFunctionality.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFunctionalityRequest$inboundSchema,
};

export const tool$catalogEntriesGetFunctionality: ToolDefinition<typeof args> =
  {
    name: "catalog-entries-get-functionality",
    description: `Get a functionality

Retrieves a single functionality by ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await catalogEntriesGetFunctionality(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
