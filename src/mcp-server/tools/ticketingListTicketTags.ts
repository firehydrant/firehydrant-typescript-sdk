/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingListTicketTags } from "../../funcs/ticketingListTicketTags.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTicketTagsRequest$inboundSchema,
};

export const tool$ticketingListTicketTags: ToolDefinition<typeof args> = {
  name: "ticketing-list-ticket-tags",
  description: `List ticket tags

List all of the ticket tags in the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingListTicketTags(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
