/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsListSignalsHeartbeatEndpointConfigurations } from "../../funcs/signalsListSignalsHeartbeatEndpointConfigurations.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$signalsListSignalsHeartbeatEndpointConfigurations:
  ToolDefinition = {
    name: "signals-list-signals-heartbeat-endpoint-configurations",
    description: `List heartbeat endpoint configurations

Retrieve all heartbeat endpoint configurations for your organization`,
    tool: async (client, ctx) => {
      const [result, apiCall] =
        await signalsListSignalsHeartbeatEndpointConfigurations(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
