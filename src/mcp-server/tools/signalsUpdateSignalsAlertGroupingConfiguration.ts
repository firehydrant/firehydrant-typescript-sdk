/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsUpdateSignalsAlertGroupingConfiguration } from "../../funcs/signalsUpdateSignalsAlertGroupingConfiguration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.UpdateSignalsAlertGroupingConfigurationRequest$inboundSchema,
};

export const tool$signalsUpdateSignalsAlertGroupingConfiguration:
  ToolDefinition<typeof args> = {
    name: "signals-update-signals-alert-grouping-configuration",
    description: `Update an alert grouping configuration.

Update a Signals alert grouping rule for the organization.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await signalsUpdateSignalsAlertGroupingConfiguration(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
