/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scimListSCIMGroups } from "../../funcs/scimListSCIMGroups.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListScimGroupsRequest$inboundSchema,
};

export const tool$scimListSCIMGroups: ToolDefinition<typeof args> = {
  name: "scim-list-scim-groups",
  description: `List SCIM groups

SCIM endpoint that lists all Teams (Colloquial for Group in the SCIM protocol)`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scimListSCIMGroups(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
