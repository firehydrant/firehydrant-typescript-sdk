/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { audiencesGetMemberDefaultAudience } from "../../funcs/audiencesGetMemberDefaultAudience.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetMemberDefaultAudienceRequest$inboundSchema,
};

export const tool$audiencesGetMemberDefaultAudience: ToolDefinition<
  typeof args
> = {
  name: "audiences-get-member-default-audience",
  description: `Get default audience

Get member's default audience`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await audiencesGetMemberDefaultAudience(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
