/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesCreateIncidentRetrospectiveField } from "../../funcs/retrospectivesCreateIncidentRetrospectiveField.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateIncidentRetrospectiveFieldRequest$inboundSchema,
};

export const tool$retrospectivesCreateIncidentRetrospectiveField:
  ToolDefinition<typeof args> = {
    name: "retrospectives-create-incident-retrospective-field",
    description:
      `Appends a new incident retrospective field to an incident retrospective

Add a new field to an incident retrospective`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await retrospectivesCreateIncidentRetrospectiveField(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
