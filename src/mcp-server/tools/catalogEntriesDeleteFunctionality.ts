/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesDeleteFunctionality } from "../../funcs/catalogEntriesDeleteFunctionality.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteFunctionalityRequest$inboundSchema,
};

export const tool$catalogEntriesDeleteFunctionality: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-delete-functionality",
  description: `Archive a functionality

Archive a functionality`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesDeleteFunctionality(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
