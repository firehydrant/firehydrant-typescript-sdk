/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsListSignalsEmailTargets } from "../../funcs/signalsListSignalsEmailTargets.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListSignalsEmailTargetsRequest$inboundSchema,
};

export const tool$signalsListSignalsEmailTargets: ToolDefinition<typeof args> =
  {
    name: "signals-list-signals-email-targets",
    description: `List email targets for signals

List all Signals email targets for a team.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await signalsListSignalsEmailTargets(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
