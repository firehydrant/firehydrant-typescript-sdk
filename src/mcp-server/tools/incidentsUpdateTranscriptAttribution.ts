/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsUpdateTranscriptAttribution } from "../../funcs/incidentsUpdateTranscriptAttribution.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTranscriptAttributionRequest$inboundSchema,
};

export const tool$incidentsUpdateTranscriptAttribution: ToolDefinition<
  typeof args
> = {
  name: "incidents-update-transcript-attribution",
  description: `Update the attribution of a transcript

Update the attribution of a transcript`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsUpdateTranscriptAttribution(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
