/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsDebugSignalsExpression } from "../../funcs/signalsDebugSignalsExpression.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.DebugSignalsExpression$inboundSchema,
};

export const tool$signalsDebugSignalsExpression: ToolDefinition<typeof args> = {
  name: "signals-debug-signals-expression",
  description: `Debug Signals expressions

Debug Signals expressions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsDebugSignalsExpression(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
