/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsListLifecycleMeasurementDefinitions } from "../../funcs/incidentSettingsListLifecycleMeasurementDefinitions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLifecycleMeasurementDefinitionsRequest$inboundSchema,
};

export const tool$incidentSettingsListLifecycleMeasurementDefinitions:
  ToolDefinition<typeof args> = {
    name: "incident-settings-list-lifecycle-measurement-definitions",
    description: `List measurement definitions

List all of the measurement definitions in the organization`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await incidentSettingsListLifecycleMeasurementDefinitions(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
