/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { callRoutesGetCallRoute } from "../../funcs/callRoutesGetCallRoute.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCallRouteRequest$inboundSchema,
};

export const tool$callRoutesGetCallRoute: ToolDefinition<typeof args> = {
  name: "call-routes-get-call-route",
  description: `Retrieve a call route

Retrieve a call route by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await callRoutesGetCallRoute(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
