/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { audiencesListAudienceSummaries } from "../../funcs/audiencesListAudienceSummaries.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAudienceSummariesRequest$inboundSchema,
};

export const tool$audiencesListAudienceSummaries: ToolDefinition<typeof args> =
  {
    name: "audiences-list-audience-summaries",
    description: `List audience summaries

List all audience summaries for an incident`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await audiencesListAudienceSummaries(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
