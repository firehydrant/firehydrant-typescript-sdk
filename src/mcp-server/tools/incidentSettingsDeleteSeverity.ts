/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentSettingsDeleteSeverity } from "../../funcs/incidentSettingsDeleteSeverity.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSeverityRequest$inboundSchema,
};

export const tool$incidentSettingsDeleteSeverity: ToolDefinition<typeof args> =
  {
    name: "incident-settings-delete-severity",
    description: `Delete a severity

Delete a specific severity`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await incidentSettingsDeleteSeverity(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
