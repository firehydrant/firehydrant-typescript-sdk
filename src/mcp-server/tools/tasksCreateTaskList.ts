/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksCreateTaskList } from "../../funcs/tasksCreateTaskList.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateTaskList$inboundSchema,
};

export const tool$tasksCreateTaskList: ToolDefinition<typeof args> = {
  name: "tasks-create-task-list",
  description: `Create a task list

Creates a new task list`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksCreateTaskList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
