/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsSearchZendeskTickets } from "../../funcs/integrationsSearchZendeskTickets.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SearchZendeskTicketsRequest$inboundSchema,
};

export const tool$integrationsSearchZendeskTickets: ToolDefinition<
  typeof args
> = {
  name: "integrations-search-zendesk-tickets",
  description: `Search for Zendesk tickets

Search for Zendesk tickets`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsSearchZendeskTickets(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
