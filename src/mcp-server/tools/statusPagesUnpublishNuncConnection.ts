/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesUnpublishNuncConnection } from "../../funcs/statusPagesUnpublishNuncConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UnpublishNuncConnectionRequest$inboundSchema,
};

export const tool$statusPagesUnpublishNuncConnection: ToolDefinition<
  typeof args
> = {
  name: "status-pages-unpublish-nunc-connection",
  description: `Unpublish a status page

Unpublish a FireHydrant hosted status page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await statusPagesUnpublishNuncConnection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
