/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { audiencesSetMemberDefaultAudience } from "../../funcs/audiencesSetMemberDefaultAudience.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SetMemberDefaultAudienceRequest$inboundSchema,
};

export const tool$audiencesSetMemberDefaultAudience: ToolDefinition<
  typeof args
> = {
  name: "audiences-set-member-default-audience",
  description: `Set default audience

Set member's default audience`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await audiencesSetMemberDefaultAudience(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
