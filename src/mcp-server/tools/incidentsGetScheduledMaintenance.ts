/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsGetScheduledMaintenance } from "../../funcs/incidentsGetScheduledMaintenance.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetScheduledMaintenanceRequest$inboundSchema,
};

export const tool$incidentsGetScheduledMaintenance: ToolDefinition<
  typeof args
> = {
  name: "incidents-get-scheduled-maintenance",
  description: `Get a scheduled maintenance event

Fetch the details of a scheduled maintenance event.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsGetScheduledMaintenance(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
