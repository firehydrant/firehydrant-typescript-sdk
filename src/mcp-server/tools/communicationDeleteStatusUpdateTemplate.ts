/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { communicationDeleteStatusUpdateTemplate } from "../../funcs/communicationDeleteStatusUpdateTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteStatusUpdateTemplateRequest$inboundSchema,
};

export const tool$communicationDeleteStatusUpdateTemplate: ToolDefinition<
  typeof args
> = {
  name: "communication-delete-status-update-template",
  description: `Delete a status update template

Delete a single status update template`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await communicationDeleteStatusUpdateTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
