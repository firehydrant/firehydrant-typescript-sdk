/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { catalogEntriesListEnvironmentServices } from "../../funcs/catalogEntriesListEnvironmentServices.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListEnvironmentServicesRequest$inboundSchema,
};

export const tool$catalogEntriesListEnvironmentServices: ToolDefinition<
  typeof args
> = {
  name: "catalog-entries-list-environment-services",
  description: `List services for an environment

List services for an environment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await catalogEntriesListEnvironmentServices(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
