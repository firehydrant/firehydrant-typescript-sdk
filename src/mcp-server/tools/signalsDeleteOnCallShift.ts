/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsDeleteOnCallShift } from "../../funcs/signalsDeleteOnCallShift.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteOnCallShiftRequest$inboundSchema,
};

export const tool$signalsDeleteOnCallShift: ToolDefinition<typeof args> = {
  name: "signals-delete-on-call-shift",
  description: `Delete an on-call shift from a team schedule

Delete a Signals on-call shift by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsDeleteOnCallShift(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
