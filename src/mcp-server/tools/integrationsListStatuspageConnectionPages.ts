/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsListStatuspageConnectionPages } from "../../funcs/integrationsListStatuspageConnectionPages.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListStatuspageConnectionPagesRequest$inboundSchema,
};

export const tool$integrationsListStatuspageConnectionPages: ToolDefinition<
  typeof args
> = {
  name: "integrations-list-statuspage-connection-pages",
  description: `List StatusPage pages for a connection

Lists available page IDs for the Statuspage integration connection.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsListStatuspageConnectionPages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
