/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { statusPagesUpdateNuncConnection } from "../../funcs/statusPagesUpdateNuncConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateNuncConnectionRequest$inboundSchema,
};

export const tool$statusPagesUpdateNuncConnection: ToolDefinition<typeof args> =
  {
    name: "status-pages-update-nunc-connection",
    description: `Update a status page

Update your company's information and other components in the specified FireHydrant hosted status page.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await statusPagesUpdateNuncConnection(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
