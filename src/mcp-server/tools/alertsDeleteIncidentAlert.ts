/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { alertsDeleteIncidentAlert } from "../../funcs/alertsDeleteIncidentAlert.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteIncidentAlertRequest$inboundSchema,
};

export const tool$alertsDeleteIncidentAlert: ToolDefinition<typeof args> = {
  name: "alerts-delete-incident-alert",
  description: `Remove an alert from an incident

Remove an alert from an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await alertsDeleteIncidentAlert(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
