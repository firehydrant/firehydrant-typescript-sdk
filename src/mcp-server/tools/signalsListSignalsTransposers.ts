/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsListSignalsTransposers } from "../../funcs/signalsListSignalsTransposers.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$signalsListSignalsTransposers: ToolDefinition = {
  name: "signals-list-signals-transposers",
  description: `List signal transposers

List all transposers for your organization`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await signalsListSignalsTransposers(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
