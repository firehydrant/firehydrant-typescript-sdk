/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { retrospectivesUpdatePostMortemReason } from "../../funcs/retrospectivesUpdatePostMortemReason.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdatePostMortemReasonRequest$inboundSchema,
};

export const tool$retrospectivesUpdatePostMortemReason: ToolDefinition<
  typeof args
> = {
  name: "retrospectives-update-post-mortem-reason",
  description: `Update a contributing factor in a retrospective report

Update a contributing factor`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await retrospectivesUpdatePostMortemReason(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
