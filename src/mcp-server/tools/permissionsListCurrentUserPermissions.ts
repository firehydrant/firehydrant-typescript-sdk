/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { permissionsListCurrentUserPermissions } from "../../funcs/permissionsListCurrentUserPermissions.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$permissionsListCurrentUserPermissions: ToolDefinition = {
  name: "permissions-list-current-user-permissions",
  description: `Get all permissions for the current user

Get all permissions for the current user`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await permissionsListCurrentUserPermissions(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
