/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsUpdateSupportHoursSchedule } from "../../funcs/signalsUpdateSupportHoursSchedule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSupportHoursScheduleRequest$inboundSchema,
};

export const tool$signalsUpdateSupportHoursSchedule: ToolDefinition<
  typeof args
> = {
  name: "signals-update-support-hours-schedule",
  description: `Update support hours schedule

Update the team's support hours schedule`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsUpdateSupportHoursSchedule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
