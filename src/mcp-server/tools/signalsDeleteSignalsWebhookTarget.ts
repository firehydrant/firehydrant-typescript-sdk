/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsDeleteSignalsWebhookTarget } from "../../funcs/signalsDeleteSignalsWebhookTarget.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSignalsWebhookTargetRequest$inboundSchema,
};

export const tool$signalsDeleteSignalsWebhookTarget: ToolDefinition<
  typeof args
> = {
  name: "signals-delete-signals-webhook-target",
  description: `Delete a webhook target

Delete a Signals webhook target by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsDeleteSignalsWebhookTarget(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
