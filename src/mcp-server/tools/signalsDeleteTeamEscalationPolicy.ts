/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signalsDeleteTeamEscalationPolicy } from "../../funcs/signalsDeleteTeamEscalationPolicy.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTeamEscalationPolicyRequest$inboundSchema,
};

export const tool$signalsDeleteTeamEscalationPolicy: ToolDefinition<
  typeof args
> = {
  name: "signals-delete-team-escalation-policy",
  description: `Delete an escalation policy for a team

Delete a Signals escalation policy by ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signalsDeleteTeamEscalationPolicy(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
