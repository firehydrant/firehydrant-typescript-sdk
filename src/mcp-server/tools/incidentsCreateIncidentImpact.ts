/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { incidentsCreateIncidentImpact } from "../../funcs/incidentsCreateIncidentImpact.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateIncidentImpactRequest$inboundSchema,
};

export const tool$incidentsCreateIncidentImpact: ToolDefinition<typeof args> = {
  name: "incidents-create-incident-impact",
  description: `Add impacted infrastructure to an incident

Add impacted infrastructure to an incident`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await incidentsCreateIncidentImpact(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
