/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsReportingGetSignalsGroupedMetrics } from "../../funcs/metricsReportingGetSignalsGroupedMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSignalsGroupedMetricsRequest$inboundSchema,
};

export const tool$metricsReportingGetSignalsGroupedMetrics: ToolDefinition<
  typeof args
> = {
  name: "metrics/reporting-get-signals-grouped-metrics",
  description: `Generate grouped alert metrics

Generate a report of grouped metrics for Signals alerts`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsReportingGetSignalsGroupedMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
